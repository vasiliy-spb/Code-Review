# Review на реализацию от [@breakmeaway](https://github.com/xcvqqz/tennis-scoreboard) проекта [Табло теннисного матча](https://zhukovsd.github.io/java-backend-learning-course/projects/tennis-scoreboard/)

```text
1. Знаком ❗️ помечены критически важные замечания, а также места нарушения ТЗ.
2. Если ❗️ стоит перед заголовком, значит он относится ко всем пунктам этого раздела.
3. Замечания, указанные в пункте с именем пакета, относятся ко всем классам этого пакета или ко всем классам этого слоя.
4. Знаком 💡 помечены блоки, в которых содержится подсказка по реализации какого-то приёма или части кода. 
   Такие пункты всегда находятся в сворачиваемом блоке и разворачиваются по нажатию. 
   Перед их раскрытием стоит постараться придумать или поискать решение самостоятельно. 
```

## Функциональный обзор
- ❗️если игрокам дать имена, которые отличаются только регистром, то создастся только один игрок и будет играть сам с собой (все очки будут учитываться ему)
- ❗️счёт в сете идёт до 7 очков вместо 6 (по ТЗ)
- когда счёт становится `40:AD` на дополнительном табло он отображается как `DEUCE:ADVANTAGE`, что не совсем корректно, потому что deuce означает равный счёт.
- нельзя использовать пробел/дефис/апостроф в имени игрока, хотя всё это может встречаться в имени — неоправдано строгое ограничение
- изменяется стиль имён игроков: `МишаМишин` становится `Мишамишин`, `КораблевЛев` -> `Кораблевлев`, что становится неожиданностью для пользователя
- последний сыгранный матч будет в конце списка на странице завершённых матчей — было бы удобнее, если бы он отображался первым

## DTO
- DTO должны быть простыми контейнерами для передачи данных между слоями (например, из сервисов в контроллеры и далее в JSP, или из контроллеров в сервисы). Они не должны содержать бизнес-логики или состояния, не относящегося к непосредственным данным, которые нужно передать.
- Хорошей практикой считается делать DTO неизменяемыми (immutable), идеально здесь — сделать DTO record-классами. 
- ❗️Сейчас классы в этом пакете нарушают SRP (Single Responsibility Principle) и смешивают ответственность DTO и доменных моделей (анемичных в текущей реализации): они передаются в слой представления (View), где из них извлекается информация, а также участвуют в бизнес-логике в сервисах. Будет лучшим подходом — разделить эту ответственность на DTO и модели (например MatchDTO и OngoingMatch).

### MatchScoreDTO (рассматривается как модель)
- названия переменных принято делать существительными (а у boolean часто и прилагательными), а префиксы `is`, `has`, `should` и подобные используют для названий методов, возвращающих boolean. Поэтому переменную лучше назвать просто `advantage`. А также сейчас её геттер называется `isHasAdvantage()`, а сеттер `setHasAdvantage()`.
- класс имеет признаки "анемичной модели" — он является лишь контейнером для данных, а вся логика находится в сервисном слое. Если бы у класса вместо простых сеттеров были методы, совершающие необходимую работу над полями, это больше соответствовало бы ООП стилю и обязанности класса (в роли доменной модели). Также, эту часть логики было бы легче тестировать.

<details>
<summary><b>💡 Вот упрощённый пример такого класса 💡</b></summary>

```java
public class MatchScore {
    private static final Point INITIAL_POINTS = Point.LOVE;
    private static final int VICTORY_TIEBREAK_POINTS = 7;
    private static final int VICTORY_SET_POINTS = 6;
    private static final int MIN_ADVANTAGE_TO_WIN = 2;
    private Point points;
    private int games = 0;
    private int sets = 0;
    private int tieBreakPoints = 0;

    public MatchScore() {
        this.points = INITIAL_POINTS;
    }

    public void increment() {
        // логика добавления очков
    }

    public boolean hasAdvantage() {
        return points == Point.ADVANTAGE;
    }

    public String getPointsValue() {
        return points.value();
    }

    private void incrementGamePoints() {
        games++;
    }

    private void incrementSetPoints() {
        sets++;
    }

    private void incrementTieBreakPoints() {
        tieBreakPoints++;
    }

    private void resetPoints() {
        points = INITIAL_POINTS;
    }

    enum Point {
        LOVE("0"),
        FIFTEEN("15"),
        THIRTY("30"),
        FORTY("40"),
        ADVANTAGE("AD");

        private final String value;

        Point(String value) {
            this.value = value;
        }

        public Point next() {
            if (this == ADVANTAGE) {
                throw new IllegalStateException("Has no points after advantage.");
            }
            return values()[ordinal() + 1];
        }

        public String value() {
            return value;
        }
    }
}
```

При таком подходе от сеттеров стоит избавиться, чтобы состояние счёта управлялось только явно предусмотренными для этого методами.

</details>

### MatchDTO (рассматривается как модель)
- этот класс также содержит признаки "анемичной модели" — является контейнером состояний матча, а также предоставляет неконтролируемое управление своими полями через публичные сеттеры
- поля `boolean openTieBreak` и `boolean deuceSituation` отражают разные состояния матча. Для этой цели (если останется такая необходимость) лучше подойдёт перечисление, например такое:
```java
public enum MatchState {
    REGULAR,
    DEUCE,
    TIEBREAK,
    FINISHED;
}
```
Использование boolean вместо перечислений делает код менее выразительным и более подверженным ошибкам.

- в качестве полноценной доменной модели (скажем, с названием OngoingMatch), класс может выглядеть так:

<details>
<summary><b>💡 Упрощённый пример 💡</b></summary>

```java
public class OngoingMatch {
    private Player playerOne;
    private Player playerTwo;
    private MatchScore playerOneScore;
    private MatchScore playerTwoScore;
    
    public OngoingMatch(Player playerOne, Player playerTwo) {
        this.playerOne = playerOne;
        this.playerTwo = playerTwo;
        this.playerOneScore = new MatchScore();
        this.playerTwoScore = new MatchScore();
    }
    
    // методы, управляющие состоянием в рамках предусмотренной логики
    
}
```
Также OngoingMatch может вообще не иметь полей для игроков (ему важен только их счёт), тогда определение какие методы вызывать (кому начислять очки) будет происходить за пределами этого класса. 

Или наоборот, содержать игроков, каждый из которых будет сам хранить объект со своим счётом (кажется, это более громоздкий вариант).

В любом случае у него тоже не должно быть простых сеттеров, а только методы, изменяющие состояние в соответствии с правилами.
</details>

### PlayerDTO (рассматривается как DTO)
- поле `MatchScoreDTO matchScoreDTO` является (фактически) объектом модели и активно участвует в бизнес-логике. Лучше, чтобы DTO были чистыми контейнерами данных и не содержали в себе доменных моделей (которые имеют поведение и участвуют в бизнес-логике). Это важно для разделения ответственности и избегания непреднамеренного изменения состояния доменного объекта при передаче данных. То есть все внутренние объекты в DTO должны быть тоже DTO.

### PaginationResponseDTO (рассматривается как DTO)
- инкапсулировать всё необходимое для пагинации в DTO — очень хороший подход, но сейчас этот класс отдаёт в представление (JSP-файлы) список из Entity (с данными напрямую из БД). Это не является хорошей практикой и может привести к проблемам производительности (например, ленивая загрузка) и безопасности (например, случайная передача чувствительных данных). Кроме того, это связывает слой представления с моделью данных, нарушая архитектурные границы. Лучше использовать список DTO, реализованных с соблюдением принципа единственной ответственности.

## Model
- название `entity` лучше подошло бы этому пакету, так как классы в нём — это сущности, связанные с БД (JPA Entity). А в пакет `model` можно поместить доменные модели. 
- ❗️`@EqualsAndHashCode` (которая входит в `@Data`) обычно не используют в JPA сущностях. Конкретно здесь проблем быть не должно, но использование этих аннотаций не является хорошей практикой.
- хорошим подходом будет оставить минимум необходимых аннотаций, достаточных для JPA Entity:
```java
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
@Entity
@Table(name = "Matches")
public class JpaEntity { 
    // ...
}
```

&#43; конструктор со всеми полями, кроме генерируемых автоматически (id), и equals/hashCode, реализованные на основе самостоятельно выбранных полей.

- если имя поля совпадает с названием колонки, то параметр name в аннотации `@Column` можно опустить: `@Column(name = "id")` -> `@Column` 
- можно добавить параметр `updatable = false` в `@Column` для поля id в качестве дополнительной защиты целостности данных: `@Column(updatable = false)`

### Player
- у класса только один параметр устанавливается в приложении — избыточно для этого генерировать builder
- аннотация `@Setter` генерирует публичный сеттер поля `private String name;`, из-за этого любой код может изменить имя игрока, что нарушает инварианты (имя игрока должно быть неизменяемым после создания). Решением будет не создавать сеттеры для полей.
- стоит добавить `nullable = false` в аннотацию `@Column`. Целостность данных должна обеспечиваться на всех уровнях: в приложении (валидация) и в БД (constraints). Отсутствие ограничений в БД означает, что данные могут быть испорчены из-за ошибок в приложении или при прямом доступе к БД.
- можно добавить параметр `length` в аннотацию `@Column`, например `length = 50`
- метод `formatName()` приводит имя к единой стандартной форме (т.е. нормализует), поэтому ещё точнее будет назвать его `normalize()`
- в методе `formatName()` можно инвертировать условие в if:
```java
if (name.length() < 2) {
    return name;
} 
```
- сейчас метод `formatName()` некорректно форматирует имена: 

  `McCartney` (Paul McCartney) он превратит в `Mccartney`, 

  а также полное `Paul McCartney` в `Paul mccartney` (хотя сейчас в проекте нельзя ввести имя и фамилию), 

  `a` НЕ превратит в `A`, 

  а при входном параметре null будет NullPointerException в `name.length()`

<details>
<summary><b>Более корректный вариант (с сохранением текущей логики — увеличивать только первую букву) может выглядеть так</b></summary>

```java
public static String formatName(String name) {
    if (name == null || name.trim().isEmpty()) {
        return name; // или выбросить исключение, но тогда нужно решить, какое
    }
    String trimmedName = name.trim();
    char[] letters = trimmedName.toCharArray();
    letters[0] = Character.toUpperCase(letters[0]);
    
    return String.valueOf(letters);
}
```

</details>

- если всегда нужно нормализовывать имя игрока перед установкой его в переменную, то можно рассмотреть использование `formatName()` в конструкторе или сеттере, чтобы форматирование имени происходило автоматически при создании игрока. В процессе рефакторинга (избавимся от race condition в PlayerService при создании нового игрока — об этом сказано ниже, в разделе [**PlayerService**](#playerservice)) доступ к этому методу извне станет не нужен.
- если принято решение нормализовывать имена (пользовательский ввод), то лучше тщательно продумать правила форматирования, иначе это ухудшит пользовательский опыт и обоснованные ожидания, что стиль написания имени не будет деградировать внутри приложения. 
- возможно, лучшим решением будет вообще отказаться от форматирования имени, потому что разработать достаточно хорошие правила нормализации может стать избыточно сложной задачей в контексте этого проекта. 

  Например, нельзя просто увеличивать первую букву каждой части имени, потому что:

  `орлов лев михайлович` -> `Орлов Лев Михайлович` — правильно,

  а `lorenzo von matterhorn` -> `Lorenzo Von Matterhorn` — нет (правильно — `Lorenzo von Matterhorn`)

  Также, это сделало бы соблюдение принципа единственной ответственности в этом классе более строгим.

### Match
- ❗️для обязательных полей стоит добавить `optional = false` в `@ManyToOne` или `nullable = false` в `@JoinColumn`. По причине, описанной в разделе [**Player**](#player)

  При `optional = false` или `nullable = false` — Hibernate генерирует `NOT NULL` ограничение в БД и делает проверку перед вставкой в БД (если значение поля null, то Hibernate не будет обращаться в БД для вставки и сам выбросит исключение PropertyValueException) 

- можно добавить ограничения, проверяющие, что игроки разные и победителем является один из участников матча

<details>
<summary><b>💡 Например так 💡</b></summary>

```java
@Checks({
        @Check(constraints = "player1 != player2"),
        @Check(constraints = "winner = player1 OR winner = player2")
})
public class Match { 
    // ...
}
```

</details>

## Repository
- JPQL запросы можно вынести в константы и дать им понятные имена, это сделает код методов ещё более читаемым
- текст запроса удобнее читать, когда он логично разбит на строки:

```sqlite-sql
SELECT p 
FROM Player p 
WHERE p.name = :name
```
- В текущей реализации методы сохранения сущностей `save(Player player)` и `save(Match match)` возвращают void. Это допустимо, но обычно в таких методах возвращают сохранённую сущность (особенно если у неё генерируется ID) или, по крайней мере, тот же объект. Это позволяет получить сгенерированный идентификатор или сохранённый объект более явно:
В PlayerService в методе `createPlayerIfNotExists()` сейчас так:
```java
Player newPlayer = Player.builder()
        .name(formattedName)
        .build();

playerRepository.save(newPlayer);
return newPlayer;
```
лучше читается так:
```java
Player newPlayer = Player.builder()
        .name(formattedName)
        .build();

Player savedPlayer = playerRepository.save(newPlayer); 
return savedPlayer; // или return playerRepository.save(newPlayer);
```
- ❗️В методах сохранения сущностей `save(Player player)` и `save(Match match)` не происходит вызова отката транзакции, при ошибке. Это может привести к неконсистентности данных и утечке ресурсов (например, блокировкам в БД). Транзакция останется открытой, и соединение может не вернуться в пул. Стоит обернуть `session.persist(entity)` в блок try-catch, и в catch вызывать `session.getTransaction().rollback()`:

```java
try {
    session.persist(match);
    session.getTransaction().commit();
} catch (Exception e) {
    if (session.getTransaction() != null && session.getTransaction().isActive()) {
        session.getTransaction().rollback();
    }
    throw new DataBaseException(e.getMessage());
}
```

<details>
<summary><b>Сейчас происходит так</b></summary>

Сейчас откат транзакции производит пул соединений (HikariCP): если во время `session.persist(entity);` происходит исключение, то происходит закрытие сессии (поскольку она создаётся в try-with-resources), а транзакция остаётся открытой. При возврате соединения в пул HikariCP обнаруживает, что на данном соединении была открыта транзакция (autoCommit отключён), и при этом она не была явно зафиксирована (commit) или отменена (rollback) — "dirty commit state" (грязное состояние транзакции). Чтобы предотвратить "протечки" незавершённых транзакций между разными сессиями, HikariCP автоматически выполняет rollback при закрытии (возврате) такого соединения. После rollback HikariCP сбрасывает состояние соединения, в частности устанавливает autoCommit обратно в true, чтобы соединение в пуле было в стандартном "чистом" состоянии для следующего использования. 

в логах это выглядит так:
```text
[main] DEBUG c.zaxxer.hikari.pool.ProxyConnection - HikariPool-1 - Executed rollback on connection conn0: url=jdbc:h2:mem:tennis-scoreboard user=SA due to dirty commit state on close().
[main] DEBUG com.zaxxer.hikari.pool.PoolBase - HikariPool-1 - Reset (autoCommit) on connection conn0: url=jdbc:h2:mem:tennis-scoreboard user=SA
```

</details>

- в обоих репозиториях есть дублирующиеся части кода и даже методы, отличающиеся только типом сущности, с которой они работают. Чтобы этого избежать стоит сделать базовый класс репозитория и вынести общую логику в него.
- сейчас в каждом методе используется `openSession()`:

  `Session session = HibernateSessionFactoryUtil.getSessionFactory().openSession();` — при таком подходе каждый раз (в каждом методе) создаётся новый объект Session.

  Вместо этого можно использовать `getCurrentSession()` — этот метод возвращает сессию текущего контекста. Так в одном потоке (HTTP-запросе) будет использоваться одна и та же сессия. В этом случае закрытие сессии тоже будет происходить автоматически (даже без try-with-resources).
  Для получения сессии через `getCurrentSession()` надо добавить в `hibernate.properties` свойство `hibernate.current_session_context_class`.

<details>
<summary><b>💡 Например, так (для режима одна-сессия-на-поток) 💡</b></summary>

```properties
hibernate.current_session_context_class=thread
```

</details>

- ❗️В текущем коде репозиториев исключения не обрабатываются. Это означает, что они будут прокидываться на уровень выше. В целом, это допустимо, если на уровне сервиса или, например, в фильтре есть обработка исключений возникающих при работе с БД (чего в нет в этой реализации — об этом будет сказано ниже).
- в текущей реализации репозитории не имеют интерфейсов. Внедрение интерфейсов позволило бы легче подменять реализации, например, для тестирования (моки) или для будущих изменений.

### PlayerRepository
- из метода `Optional<Player> findByName(String name)` можно сразу возвращать результат:
```java
private static final String FIND_BY_NAME_JPQL = """
        FROM Player
        WHERE name = :name
        """;

public Optional<Player> findByName(String name) {
    try (Session session = HibernateUtil.getSessionFactory().openSession()) {
        return session.createQuery(FIND_BY_NAME_JPQL, Player.class)
                .setParameter("name", name)
                .uniqueResultOptional();
    }
}
```

### MatchRepository
- в названиях методов `countFinishedMatches()` и `findFinishedMatches()` лучше не использовать суффиксы `Finished` (потому что репозиторий не знает и не должен знать, в каком именно состоянии матчи он хранит) и `Matches` (потому что этот контекст понятен из названия класса, а в методе поиска ещё и из типа возвращаемого значения). Можно дать этим методам более лаконичные имена, например: `countAll()` и `findAll()`.
- в методе `countFinishedMatches()` сконструированный JPQL запрос собирается в переменную `StringBuilder sb` — лучше дать ей более понятное имя, например, как в методе `findFinishedMatches()` — `hql`. Или везде назвать `queryBuilder`.
- в методе `findFinishedMatches()` alias сущности в тексте запроса оторван от объявления выборки: `"FROM Match"` соединяется с `" m WHERE ..."` — если пробел перед второй частью потеряется (например, в процессе рефакторинга), то возникнет ошибка. Если нужно разделить текст запроса на части, то лучше делать это по целым частям, а также более выразительно указывать разделительный символ: `"FROM Match m\n"` + `"WHERE ..."`.
- также сейчас в методах `countFinishedMatches()` и `findFinishedMatches()` при каждом вызове создаются новые объекты StringBuilder, конструирующие необходимый запрос. Лучшим решением будет вынести все запросы в константы, чтобы они создавались один раз и в нужном месте просто подставлять подходящий запрос. Это сделает код более чистым и читаемым, а также чуть увеличит производительность:
```java
private static final String SELECT_ALL_QUERY = """
        FROM Match m
        """;
private static final String COUNT_ALL_QUERY = """
        SELECT COUNT(m)
        FROM Match m
        """;
private static final String NAME_FILTER_JPQL = """
        WHERE m.playerOne.name ILIKE :playerName
            OR m.playerTwo.name ILIKE :playerName
        """;
private static final String SELECT_ALL_FILTERED_QUERY = SELECT_ALL_QUERY + NAME_FILTER_JPQL;
private static final String COUNT_ALL_FILTERED_QUERY = COUNT_ALL_QUERY + NAME_FILTER_JPQL;
```
- из метода `countFinishedMatches()` вместо `query.getResultStream().findFirst().orElse(0L);` можно возвращать `query.getSingleResult();`
- в текущем коде, повторяющуюся 4 раза в классе проверку `!playerName.trim().isEmpty() || !playerName.isBlank()`, можно вынести в метод с понятным названием. Но само условие проверки избыточно — метод `String.isBlanck()` вернёт то же, что и `String.trim().isEmpty()`. Более полная проверка наличия фильтра по имени будет выглядеть так:
```java
private boolean hasFilterByName(String playerName) {
    return playerName != null && !playerName.isBlank();
}
```
- будет более правильным подходом вынести проверку наличия фильтра в сервис, а в репозитории реализовать методы под необходимые варианты запросов. Это также будет точнее соответствовать принципу единственной ответственности: репозиторий не должен принимать решений о том, что значит пустой параметр — его задача предоставлять доступ к данным.
- в запросе используется `LIKE :playerName`, но параметр имени устанавливается без необходимого паттерна `query.setParameter("playerName", playerName);`, поэтому поиск по части имени не работает. Чтобы исправить это надо устанавливать параметр имени по паттерну `"%playerName%"`, допуская любые символы до и после введённой части имени -> `query.setParameter("playerName", "%" + playerName + "%")` 
- ❗️В методе `findFinishedMatches()` загружаются сущности Match, которые связаны с Player отношением многие-к-одному. В текущей реализации при загрузке одного матча будет выполняться ещё два запроса для загрузки связанных с ним игроков (проблема N+1). Следует это исправить — например, изменить запросы так, чтобы сразу загружать связанные сущности.

<details>
<summary><b>💡 Вот пример такого запроса 💡</b></summary>

Оптимизируем запросы с помощью JOIN FETCH
```java
String FIND_BY_NAME_JPQL = """
        SELECT m FROM Match m
        LEFT JOIN FETCH m.playerOne
        LEFT JOIN FETCH m.playerTwo
        LEFT JOIN FETCH m.winner
        WHERE m.playerOne.name LIKE :playerName OR m.playerTwo.name LIKE :playerName
        """;
```

</details>

## Service
- неоднородность в создании шаблонного кода: в классах PlayerService и OngoingMatchesService конструктор без параметров создаётся через Lombok, а в классе FinishedMatchesPersistenceService — вручную. 
- Жёсткая связь через `new` (создание объектов репозиториев прямо в полях классов) ограничивает гибкость и тестируемость кода. Внедрение зависимостей через конструктор — это способ устранить эти недостатки.

### PlayerService
- аннотации `@Builder` и `@AllArgsConstructor` можно удалить — в текущей реализации они не используются
- аннотация `@Builder.Default` также не нужна над полем `PlayerRepository playerRepository`
- в названии метода `createPlayerIfNotExists()` можно не указывать инфикс `Player` — этот контекст понятен из типа возвращаемого значения
- аналогично в названии метода `createOrGetPlayerDTO()` можно не указывать суффикс `PlayerDTO` — этот контекст понятен из типа возвращаемого значения
- метод `createPlayerIfNotExists()` можно сделать ещё более читаемым:
```java
private Player createPlayerIfNotExists(String name) {
    String formattedName = Player.formatName(name);
    return playerRepository.findByName(formattedName)
            .orElseGet(() -> create(formattedName));
}

private Player create(String name) {
    Player player = new Player(name); // здесь мы уже избавились от builder-а в классе Player  
    playerRepository.save(player);
    return player;
}
```
- ❗️Race condition при создании игрока — сначала пытаемся найти игрока по имени и, если его нет, делаем ещё один запрос, чтобы сохранить:
```java
private Player createPlayerIfNotExists(String name) {
    String formattedName = Player.formatName(name);
    return playerRepository.findByName(formattedName)
            .orElseGet(() -> {
                Player newPlayer = Player.builder()
                        .name(formattedName)
                        .build();

                playerRepository.save(newPlayer);
                return newPlayer;
            });
}
```

Между поиском и сохранением игрока другой поток может создать игрока с тем же именем. И тогда при сохранении возникнет исключение, которе нигде не обрабатывается и пользователь увидит страницу с кодом 500. Хотя создать матч с уже существующим в БД игроком — нормальная ситуация. Вместо этого надо сразу пытаться сохранить игрока, позволить БД выбросить ошибку, если такой игрок уже существует, и тогда уже обработать этот случай, продолжив работу приложения.
Кроме спасения от race condition, это уменьшит количество запросов, а значит снизит нагрузку на БД.

После рефакторинга по предложению из этого пункта доступ к методу `Player.formatName()` cтанет не нужен извне (исчезнет необходимость получить форматированное имя, чтобы искать по нему игрока) и логику нормализации имени (если она останется в проекте) можно будет полностью закрыть в классе Player.

### FinishedMatchesPersistenceService
- пустой публичный конструктор без параметров можно не писать в коде, если он единственный в классе — в таком случае в java он будет по умолчанию
- константы принято объявлять первыми в классе — самыми верхними полями
- параметр размера страницы лучше принимать в конструктор. В идеале он выбирается пользователем и приходит с фронтенда, затем сервис, отталкиваясь от его значения, запрашивает нужную выборку из репозитория. Несмотря на то, что в JSP сейчас нет возможности выбрать количество матчей на странице, сервис не должен хранить это значение как собственную константу. 
- логика определения фильтра по имени (есть или нет) будет уместна в этом сервисе. И в зависимости от запроса именно отсюда можно вызывать соответствующий метод репозитория. 
- в этом сервисе будет правильным — произвести конвертацию (через маппер) Entity-матчей, полученных из БД в матчи-DTO, которые будут отданы через контроллеры в представление (JSP).
- в методе `getFinishedMatches()` есть риск несогласованности данных из-за race condition при получении списка матчей. Выполнение запросов о количестве матчей и получение самих матчей должно происходить в одной транзакции. Сейчас между `matchRepository.countFinishedMatches(formattedName)` и `matchRepository.findFinishedMatches(formattedName, offset, pageSize)` количество матчей может измениться (из другого потока в результате параллельного запроса от другого пользователя) и возникнет несогласованность между фактическим количеством матчей `List<Match> matches` и значением счётчика `Long totalMatches`:
```java
public PaginationResponseDTO getFinishedMatches(String playerName, Long currentPage){
      // ...  
        
      Long totalMatches = matchRepository.countFinishedMatches(formattedName);
        
      // здесь может произойти изменение количества подходящих матчей в БД  

      List<Match> matches =  matchRepository.findFinishedMatches(formattedName,  offset, pageSize);

      return new PaginationResponseDTO(matches, totalPages);
}
```
Избежать этого можно подняв управление транзакциями на уровень сервисов — и задавать нужное поведение через `@Transactional`

<details>
<summary><b>💡 Это можно сделать так 💡</b></summary>

```java
@Transactional // Transactional.TxType.REQUIRED — используется по умолчанию
public PaginationResponseDTO getFinishedMatches(String playerName, Long currentPage) {
    // тело метода
}
```

Это гарантирует, что:
- если транзакция уже есть, метод выполнится в её рамках
- если нет, то транзакция начнётся перед выполнением метода и будет коммититься после его завершения
- при возникновении необработанного исключения транзакция будет откачена

</details>

### OngoingMatchesService
- аннотация `@NoArgsConstructor` не нужна в этом классе, так как в java, если в классе не объявлен другой конструктор, то публичный конструктор без параметров будет по умолчанию
- ❗️HashMap не является потокобезопасной структурой данных, поэтому его лучше не использовать в качестве хранилища матчей. Корректно будет использовать ConcurrentHashMap
- из названий методов `createNewOngoingMatch()`, `deleteOngoingMatch()`, `getOngoingMatch()`, `addNewOngoingMatches()` можно убрать суффикс `OngoingMatch` — этот контекст понятен из названия класса, а также из типа возвращаемого значения или аргументов  
- из названий методов `createNewOngoingMatch()`, `addNewOngoingMatches()` можно убрать инфикс `New` — он не несёт значимой смысловой нагрузки
- id матча может создаваться в методе `createNewOngoingMatch()` — не имеет смысла принимать его в качестве аргумента. А вот возвращать id созданного матча из этого метода было бы очень уместным.
- метод 
```java
private void addNewOngoingMatches(UUID uuid, MatchDTO matchDTO) {
    ongoingMatches.put(uuid, matchDTO);
}
```
вызывается только в одном месте из этого же класса — его можно упразднить и там где нужно напрямую класть матч в хранилище через `ongoingMatches.put(uuid, matchDTO)`
- сейчас в методе `createNewMatchDTO()` создаётся пустая сущность Match, которая сразу преобразуется в DTO и только после этого в него устанавливаются игроки.
  Лучшим решением будет упразднить метод `createNewMatchDTO()` и немного изменить декларацию метода `toDTO()` в интерфейсе MatchMapper.

<details>
<summary><b>💡 Например так 💡</b></summary>

```java
public interface MatchMapper {
    // другие методы
  
      // было
//    @Mapping(source = "playerOne", target = "playerOne")
//    @Mapping(source = "playerTwo", target = "playerTwo")
//    @Mapping(target = "winner", ignore = true)
//    MatchDTO toDTO(Match match);
  
    // стало
    @Mapping(target = "winner", ignore = true)
    MatchDTO toDTO(PlayerDTO playerOne, PlayerDTO playerTwo);
}
```

на основе этого будет сгенерирована такая реализация
```java
@Override
public MatchDTO toDTO(PlayerDTO playerOne, PlayerDTO playerTwo) {
    if ( playerOne == null && playerTwo == null ) {
        return null;
    }

    MatchDTO.MatchDTOBuilder matchDTO = MatchDTO.builder();

    matchDTO.playerOne( playerOne );
    matchDTO.playerTwo( playerTwo );

    return matchDTO.build();
}
```

и в классе OngoingMatchesService там, где сейчас используется `createNewMatchDTO()`, можно будет напрямую вызвать `matchMapper.toDTO(playerOneDTO, playerTwoDTO)`
```java
public void createNewOngoingMatch(UUID uuid, PlayerDTO playerOneDTO, PlayerDTO playerTwoDTO){
    // MatchDTO matchDTO = createNewMatchDTO(playerOneDTO, playerTwoDTO); // было
        
    MatchDTO matchDTO = matchMapper.toDTO(playerOneDTO, playerTwoDTO); // стало

    ongoingMatches.put(uuid, matchDTO);
}
```

Это полностью инкапсулирует логику преобразования в маппере и сделает код класса OngoingMatchesService чище, а его ответственность (в соответствии с Single Responsibility Principle) более чёткой

</details>

- ❗️метод `void delete(MatchDTO match)` никогда не удаляет сущности из хранилища:

  ключом в `Map<UUID, MatchDTO> ongoingMatches` является UUID, а метод пытается удалить матч по значению — MatchDTO:

```java
Map<UUID, MatchDTO> ongoingMatches = new HashMap<>();
public void delete(MatchDTO match) {
    ongoingMatches.remove(match);
}
```
В результате этого все матчи хранятся бессрочно — это будет создавать всё нарастающую утечку памяти.

Решением будет — изменить сигнатуру метода

<details>
<summary><b>💡 Вот так 💡</b></summary>

метод должен принимать объект, который является ключом в хранилище — id матча, который нужно удалить
```java
public void delete(UUID matchId) {
    ongoingMatches.remove(matchId);
}
```

</details>

- объект маппера хранится в статическом поле класса MatchMapper и используется в этом сервисе только в одном месте, поэтому в текущей реализации не имеет смысла хранить его в поле — можно в месте вызова прямо обращаться к `MatchMapper.INSTANCE`  
- Класс можно существенно упростить, выполнив предложенный рефакторинг
<details>
<summary><b>💡 После чего он может выглядеть так 💡</b></summary>

```java
public class OngoingMatchesService {
    private static final Map<UUID, MatchDTO> ongoingMatches = new ConcurrentHashMap<>();

    public UUID create(PlayerDTO playerOneDTO, PlayerDTO playerTwoDTO) {
        UUID matchId = UUID.randomUUID();
        MatchDTO matchDTO = MatchMapper.INSTANCE.toDTO(playerOneDTO, playerTwoDTO);

        ongoingMatches.put(matchId, matchDTO);
        return matchId;
    }

    public void delete(UUID matchId) {
        ongoingMatches.remove(matchId);
    }

    public MatchDTO get(UUID matchId) {
        return ongoingMatches.get(matchId);
    }
}
```

</details>

### MatchScoreCalculationService
- builder, который генерируется с помощью аннотации `@Builder` используется только в тестах. Поскольку в классе всего одно non-static поле, cоздавать для него builder избыточно. Аннотацию `@Builder` можно удалить, а в тесте (в текущей реализации) использовать создание объекта через конструктор.
- ❗️поле `MatchDTO matchDTO` объявлено как final и класс принимает его в конструктор — это делает каждый объект этого сервиса жёстко связанным с объектом текущего матча. И для каждого нового матча потребуется новый объект сервиса — это породит избыточное создание объектов. Правильным подходом будет сделать так, чтобы сервис мог работать с любым матчем (например, принимая его в качестве параметра в методы).
- константы принять объявлять первыми в классе — самыми верхними полями
- хотя объект, с которым работает этот сервис и имеет в названии суффикс `DTO`, фактически он выполняет (не в полной мере) роль доменной модели.
- сервис слишком много знает о внутреннем устройстве класса MatchScoreDTO, а также полностью управляет внутренним состоянием MatchDTO, что повышает связанность кода и делает его сложнее в поддержке и рефакторинге. Решение — реализация методов для работы с собственными данными в классах доменной модели. Также имеет смысл перейти на реализацию в ООП стиле — провести детальную декомпозицию предметной области, выделить соответствующие абстракции и наделить их нужным состоянием и поведением.
- количество возможных значений счёта в теннисе ограничено, поэтому для его хранения лучше подойдёт перечисление (enum), чем набор численных переменных
- жёстко закодированные правила игры (константы с количеством очков в сете, тай-брейке и т.д.) делают систему негибкой и невозможной для переиспользования без изменения архитектуры
- класс отвечает за логику очков во всех возможных ситуациях в матче — это слишком большая ответственность для одного класса, а также нарушает SRP (Single Responsibility Principle).
- в классе есть несколько магических чисел: `67| if(scoringSide.getGames() == 7)` `75| if(scoringSide.getSets() == 2)` — стоит вынести их в константы с понятными названиями.
- ❗️есть побочные эффекты. Например, метод `isWinner()` кроме определения есть ли победитель ещё и устанавливает его в `matchDTO`. Побочные эффекты в методах делают код непредсказуемым и сложным для отладки и поэтому не являются хорошей практикой.
- метод `isWinner()` можно упразднить, потому что он определяет, завершён ли матч, что можно сделать в одну строку `matchDTO.isMatchOver()`, а установка победителя в `matchDTO` не является его ответственностью и не должна происходить в этом методе.  
- название аргумента в методе `void addPoint(PlayerDTO getsPlayerPoint)` не информативное, лучше назвать его, например `player`
- метод `void addAdvantage(MatchScoreDTO scoringSide, MatchScoreDTO opposingSide)` принимает в качестве аргументов два одинаковых объекта и у первого устанавливает поле `hasAdvantage` в true, а у второго в false

```java
private void addAdvantage(MatchScoreDTO scoringSide, MatchScoreDTO opposingSide) {
    scoringSide.setHasAdvantage(true);
    opposingSide.setHasAdvantage(false);
}
```

Этот метод невозможно правильно использовать, не заглядывая в его реализацию: в момент вызова не понятно, кому именно установится преимущество.

Стоит разделить его на два метода с понятным по сигнатуре поведением:
```java
private void addAdvantage(MatchScoreDTO score) {
    score.setHasAdvantage(true);
}

private void lostAdvantage(MatchScoreDTO score) {
    score.setHasAdvantage(false);
}
```
И, заметив, что они стали выполнять всего одно действие, а также вызываются только один раз — упразднить их и устанавливать преимущество напрямую (говоря только в контексте текущей реализации) там, где это необходимо

- в методе `handleTieBreakSituation()` дважды подряд обнуляются очки у обеих сторон:
```java
scoringSide.setGames(RESET_SCORE);
opposingSide.setGames(RESET_SCORE);
scoringSide.setGames(RESET_SCORE);
opposingSide.setGames(RESET_SCORE);
```
- методу `incrementPoints()` в текущей реализации больше подошло бы название `incrementNumericPoints()` потому что он обрабатывает очки только от `0` до `40`
- сложные для чтения условия вроде `if (scoringSide.getTieBreakPoints() >= TIEBREAK_WINNER_POINTS && hasTwoPointLead(scoringSide, opposingSide))` лучше выносить в отдельные методы с понятным названием вида:
```java
private boolean isSpecificCondition(Argument one, Argument other) {
    // return checking result
}
```
- методы `handlePoint()`, `handleDeuceSituation()` и `handleTieBreakSituation()` имеют несколько разных ответственностей и ветвистую логику, что делает их поддержку избыточно сложной задачей. Это следствие архитектурных недостатков доменных моделей.  
- ❗️в методе `checkSetWin()` есть несоответствующее ТЗ условие `if (scoringSide.getGames() == 7)` — по правилам тенниса, чтобы выиграть сет, нужно выиграть 6 геймов (с разницей в 2 гейма).
- подсчёт очков является ядром бизнес-логики приложения, поэтому стоит перепроектировать эту функциональность с учётом всех предложенных рекомендаций

## Controller
### BasicController
- ❗️в классе создаются экземпляры сервисов и UUIDUtil, которые используются не всеми наследниками (например, IndexController не использует их вообще). Это порождает избыточное создание объектов: если наследник не использует сервис, то он все равно создаётся. Хорошим подходом будет объявлять необходимые поля только в том контроллере, где они используются. Чтобы при этом сервисы в приложении существовали только в одном экземпляре, можно сделать их синглтонами или создавать один раз при старте приложения и помещать в контекст, а в нужном контроллере внедрять их в поле через метод `public void init(ServletConfig config)` (переопределяя его от HttpServlet).    
- сейчас для forward-а на каждую существующую в проекте JSP существует отдельный метод. Это значит, что при добавлении нового контроллера придётся менять и базовый класс (BasicController). Идея вынести общий для всех контроллеров код в родителя правильная и можно реализовать её более гибким образом

<details>
<summary><b>💡 Например так 💡</b></summary>

```java
private static final String PATH_DELIMITER = "/";
private static final String JSP_EXTENSION = ".jsp";

protected void forwardTo(
        String pageName,
        HttpServletRequest request,
        HttpServletResponse response
) throws ServletException, IOException {

    request.getRequestDispatcher(PATH_DELIMITER + pageName + JSP_EXTENSION)
            .forward(request, response);
}
```

Это сделает класс готовым к любым новым контроллерам и значительно сократит повторяющийся код 

</details>

- также можно сделать универсальный метод для редиректа

<details>
<summary><b>💡 Например так 💡</b></summary>

```java
private static final String PATH_DELIMITER = "/";
private static final String PARAMETER_PREFIX = "?";
private static final String PARAMETER_EQUAL = "=";
private static final String PARAMETER_DELIMITER = "&";

protected void redirectTo(
        String subPath,
        Map<String, ?> requestParameters,
        HttpServletRequest request,
        HttpServletResponse response
) throws IOException {

    String parameters = buildParameters(requestParameters);
    response.sendRedirect(request.getContextPath() + PATH_DELIMITER + subPath + parameters);
}

private String buildParameters(Map<String, ?> requestParameters) {
    if (requestParameters.isEmpty()) {
        return "";
    }

    return PARAMETER_PREFIX +
           requestParameters.entrySet().stream()
                   .filter(this::isValidEntry)
                   .map(this::buildParameter)
                   .collect(Collectors.joining(PARAMETER_DELIMITER));
}

private boolean isValidEntry(Map.Entry<String, ?> entry) {
    return isNotEmpty(entry.getKey()) && isNotEmpty(entry.getValue());
}

private boolean isNotEmpty(Object object) {
    return object != null && !object.toString().isBlank();
}

private String buildParameter(Map.Entry<String, ?> parameterEntry) {
    String encodedKey = encodeForUrl(parameterEntry.getKey());
    String encodedValue = encodeForUrl(parameterEntry.getValue().toString());

    return encodedKey + PARAMETER_EQUAL + encodedValue;
}

private String encodeForUrl(String raw) {
    return URLEncoder.encode(raw, StandardCharsets.UTF_8);
}
```

</details>

### NewMatchController
- сейчас в методе `doPost()` вызываются три метода для проверки имени игроков
```java
Validator.validate(playerOneName);
Validator.validate(playerTwoName);
Validator.validateNamesUniqueness(playerOneName, playerTwoName);
```
Можно реализовать валидатор так, чтобы он принимал в качестве аргумента DTO состоящий из имён двух игроков и инкапсулировать в нём все необходимые проверки. Это упростит метод `doPost()`

<details>
<summary><b>💡 DTO может выглядеть так 💡</b></summary>

```java
public record CreateMatchDto(
        String firstPlayerName,
        String secondPlayerName
) {
}
```

</details>

- id матча может генерироваться в OngoingMatchesService, а затем возвращаться из метода `createNewOngoingMatch()` (об этом уже сказано в разделе [**OngoingMatchesService**](#ongoingmatchesservice))
- сейчас контроллер уже использует слой сервисов и это хорошо. Было бы идеально, если бы он вызывал только один метод и только у одного сервиса. То есть создал DTO из имён игроков, передал бы его сервису и получил от него id cозданного матча. А создание игроков и передача их в OngoingMatchesService происходило в слое сервисов. Это сделало бы более строгим следование принципу единственной ответственности (Single Responsibility Principle, SRP) в этом классе. 

### MatchScoreController
- в методах `doGet()` и `doPost()` нет обработки NullPointerException и IllegalArgumentException, которые могут произойти в строке `UUID uuid = uuidUtil.parseUUID(request.getParameter("uuid"))`
- использование `UUIDUtil` может иметь смысл, если в нём проверяются входные параметры и обрабатываются исключения от UUID. Но сейчас этого не происходит и можно с тем же результатом вызывать статический метод `fromString()` напрямую у UUID. 
- ❗️в методе `doPost()` происходит создание объекта MatchScoreCalculationService 
```java
/* 33 строка */ MatchScoreCalculationService matchScoreCalculationService = new MatchScoreCalculationService(match);
```
Это значит, что при каждом нажатии кнопки `Point for [PlayerName]` на фронтенде здесь содаётся новый объект MatchScoreCalculationService. Таким образом новый объект сервиса создаётся не просто для каждого объекта матча (с которым он жёстко связан), но и для каждого выигранного очка. Это делает создание избыточных объектов чрезмерным. Перепроектирование главной бизнес-логики приложения (предложенное в разделе [**MatchScoreCalculationService**](#matchscorecalculationservice)) сможет полностью решить эту проблему. 
- для метода `doPost()` было бы идеально парсить параметры запроса, передавать их в сервис и получать результат. Чтобы логикой определения, какой именно игрок выиграл очко, занимался сервисный слой. Это сделало бы более строгим следование принципу единственной ответственности (Single Responsibility Principle, SRP) в этом классе.
- из объявления метода `doPost()` лучше удалить исключение, которое он не выбрасывает
- имени переменной `boolean isPlayerOne` можно добавить больше информативности и убрать вопросительный префикс — например, `playerOneWonPoint`
- в методе `doPost()` можно немного уменьшить вложенность
Вместо
```java
if(match.isMatchOver()){
    finishedMatchesPersistenceService.save(match);
    ongoingMatchesService.deleteOngoingMatch(match);
    request.setAttribute("playerWinner", playerName);
    sendRedirectToWinnerMatch(request, response, playerName);
} else {
    sendRedirectToMatchScore(request, response, uuid);
}
```
сделать так
```java
if (!match.isMatchOver()) {
    sendRedirectToMatchScore(request, response, uuid);
    return;
}

finishedMatchesPersistenceService.save(match);
ongoingMatchesService.deleteOngoingMatch(match);
request.setAttribute("playerWinner", playerName);
sendRedirectToWinnerMatch(request, response, playerName);
```

### FinishedMatchesController
- валидация (и установка по умолчанию) параметров, которые приходят в метод `doGet()` происходит в MatchesParamFilter. Это может быть хорошим учебным упражнением, однако создавать фильтр для валидации двух параметров — избыточно. Притом, что валидация номера страницы происходит повторно в этом же контроллере в методе `parsePageParameter()`. 
- вместо установки атрибутов из DTO по отдельности можно (и лучше) передавать в JSP сам DTO (в который можно добавить поле `page` для текущей страницы и `filter` для фильтра по имени)
и тогда вместо
```java
request.setAttribute("page", page);
request.setAttribute("playerName", playerName);
request.setAttribute("matches", paginationResponseDTO.getMatches());
request.setAttribute("totalPages", paginationResponseDTO.getTotalPage());
```
достаточно будет установить один атритбут, например такой
```java
request.setAttribute("paginationResponse", paginationResponseDTO);
```
При таком подходе надо будет изменить JSP соответствующим образом — на использование PaginationResponseDTO.

## Filter
### MatchesParamFilter
- в методе `doFilter()` дублируется блок кода
```java
if (name.equals("page")) {
    String page = super.getParameter("page");
    return (page == null) ? "1" : page;
}
```
- этот класс можно упразднить. По причине, указанной в разделе [**FinishedMatchesController**](#finishedmatchescontroller), у этого него недостаточно причин для существования в проекте.

### ExceptionHandlingFilter
- ❗️сейчас фильтр обрабатывает только запросы на `/new-match` — следует расширить его обязанность на все пути проекта и централизовано обрабатывать все ошибки приложения
- ❗️после реализации в проекте всеобъемлющей обработки исключений, в этот фильтр стоит добавить конвертацию всех возможных ошибок в понятный для пользователя вид 
- будет хорошим подходом создать отдельную JSP, которая будет в доступном формате сообщать пользователю о том, что пошло не так (в случае, когда нельзя оставить пользователя на текущей странице или показать запрашиваемую) и делать forward из метода `sendError()` на неё 
- ❗️в текущей реализации сообщение об ошибке (`e.getMessage()`) передается напрямую в JSP. Это может привести к раскрытию внутренней информации, которую не следует показывать пользователю. Стоит использовать специализированные сообщения об ошибках для пользователя, а детализированные логи оставить для записи в лог-файл.

## Mapper
- можно вынести пакет mapper из util в корневой пакет проекта (на тот же уровень, что и service, repository, controller и т.д.). Потому что мапперы — это не просто утилиты, они являются частью слоя преобразования данных (data transformation layer) и выполняют важную бизнес-логику преобразования.

### MatchMapper
- декларацию метода `MatchDTO toDTO(Match match)`
```java
@Mapping(source = "playerOne", target = "playerOne")
@Mapping(source = "playerTwo", target = "playerTwo")
@Mapping(target = "winner", ignore = true)
MatchDTO toDTO(Match match);
```
можно изменить на предложенный в разделе [**OngoingMatchesService**](#ongoingmatchesservice) вариант

<details>
<summary><b>💡 Вот так 💡</b></summary>

```java
@Mapping(target = "winner", ignore = true)
MatchDTO toDTO(PlayerDTO playerOne, PlayerDTO playerTwo);
```

</details>

- названия полей в классе Match совпадают с названиями в MatchDTO, поэтому из декларации метода `toEntity()` можно убрать все аннотации. Убедиться в этом и посмотреть сгенерированный Mapstruct код можно в пакете `target/generated-sources/annotations/io/github/xcvqqz/tennis_scoreboard/util/mapper/` после компиляции (`mvn compile`).   
- в текущей реализации метод `toDTO()` игнорирует поле `winner`. Это происходит потому что в OngoingMatchesService и MatchScoreCalculationService (где MatchDTO используется как доменная модель) матч еще не завершен. После реализации классов DTO в соответствии с принципом единственной ответственности нужно будет убедиться, что маппинг Entity -> DTO не приводит к потере данных (т.е. не игнорировать поля в DTO).

### PlayerMapper
- названия полей в классе PlayerDTO совпадают с названиями в Player, поэтому из декларации метода `toDTO()` можно убрать все аннотации. Убедиться в этом и посмотреть сгенерированный Mapstruct код можно в пакете `target/generated-sources/annotations/io/github/xcvqqz/tennis_scoreboard/util/mapper/` после компиляции (`mvn compile`).

## Util
### DataBaseInitalizer
- опечатка в названии класса: `DataBaseInitalizer` -> `DataBaseInitializer`
- метод `execute()` используется только внутри класса, поэтому стоит сделать его private
- слишком сложная и нечитаемая конструкция:
```java
sql = new String(Files.readAllBytes(Paths.get(Objects.requireNonNull(
        HibernateUtilOld.class.getClassLoader().getResource(dataBasePath)).toURI())));
```
В таких случаях лучше вынести логику в отдельный метод и придерживаться правила "одна операция — одна строка":
```java
private static String readResource(String path) {
    ClassLoader classLoader = Thread.currentThread().getContextClassLoader(); // используем контекстный ClassLoader
    
    try (InputStream inputStream = classLoader.getResourceAsStream(path)) {
        if (inputStream == null) { // вместо `Objects.requireNonNull()` добавим явную проверку и понятное сообщение в случае ошибки
            throw new IllegalArgumentException("Resource not found: " + path);
        }

        return new String(inputStream.readAllBytes(), StandardCharsets.UTF_8);
    } catch (IOException e) {
        throw new UncheckedIOException("Error reading resource: " + path, e);
    }
}
```
- ❗️если в методе `isDataBaseEmpty()` возникнет исключение, то оно будет выброшено наружу, без обработки
- ❗️риск race condition в методе `execute()`: если несколько потоков одновременно проверят, что БД пустая, то каждый попытаеся наполнить её данными и приложение упадёт с ошибкой (при попытке одним из потоков добавить игрока с уже существующим в БД именем).
- ❗️в методе `execute()` в блоке
```java
} catch (Exception e) {
    session.getTransaction().rollback();
    throw new DataBaseException(DB_ERROR);
}
```
теряется конкретная причина SQL ошибки
- этот класс можно упразднить и позволить Hibernate автоматически инициализировать БД после создания схемы.

<details>
<summary><b>💡 Вот так 💡</b></summary>

Более простой способ наполнить БД тестовыми данными — положить SQL-файл в `src/main/resources/import.sql` и Hibernate автоматически выполнит `import.sql` после создания схемы.
Только для этого надо отформатировать его таким образом
```sqlite-sql
INSERT INTO players (name) VALUES ('Max'), ('Ivan'), ('Egor'), ('Igor');

INSERT INTO matches (player1, player2, winner) VALUES (1,3,3), (2,4,2), (3,1,1), (4,2,2), (2,1,1), (4,3,4), (2,3,2);
```

А также можно оставить прежний SQL-файл на прежнем месте и просто задать в `hibernate.properties` свойство
```properties
hibernate.hbm2ddl.import_files=sql_script/init_database.sql
```

Но нужно будет всё равно отформатировать файл.

</details>

### HibernateUtil
- `Properties hibernateProperties` в качестве статического поля не нужно этому классу — достаточно будет объявить такую переменную в методе `getSessionFactory()`
- ❗️в методе `getSessionFactory()` нет проверки на null при отсутствии файла `hibernate.properties` — риск NullPointerException
- ❗️Вызов `addAnnotatedClass()` сообщает Hibernate о классе, который является сущностью (т.е. помечен `@Entity`) и который мы хотим сопоставить с таблицей в БД. Класс Optional не является JPA Entity, поэтому его не нужно добавлять в конфигурацию для маппинга.
- ❗️в обязанность класса HibernateUtil и метода `getSessionFactory()` не должен входить запуск заполнения БД тестовыми данными 
- ❗️риск race condition при создании SessionFactory. В текущей реализации SessionFactory создаётся лениво (lazy) и без синхронизации, значит два потока могут параллельно создать две фабрики. Чтобы избежать этого можно использовать synchronized, статический блок инициализации или внутренний статический holder-class.
- ❗️в блоке
```java
} catch (Exception e) {
    shutdown();
    throw new DataBaseException(HIBERNATE_CONFIGURATION_ERROR);
}
```
теряется stacktrace и сообщение оригинального исключения из-за чего диагностировать причину ошибки будет невозможно
- метод `buildSessionFactory()` можно записать более последовательно (+ добавим предложенные изменения)

```java
private static SessionFactory buildSessionFactory() {
    try {
        Properties properties = new Properties();
        try (InputStream inputStream = HibernateUtil.class
                .getClassLoader()
                .getResourceAsStream(HIBERNATE_PROPERTIES)) {

            if (inputStream == null) {
                throw new DataBaseException(HIBERNATE_PROPERTIES + " not found in resources");
            }

            properties.load(inputStream);
        }

        ServiceRegistry serviceRegistry = new StandardServiceRegistryBuilder()
                .applySettings(properties)
                .build();

        return new Configuration()
                .setProperties(properties)
                .addAnnotatedClass(Player.class)
                .addAnnotatedClass(Match.class)
                .buildSessionFactory(serviceRegistry);

    } catch (Exception e) {
        throw new DataBaseException("Error initializing Hibernate: " + e.getMessage());
    }
}
```

- создать SessionFactory можно более лаконичным способом по примеру из [Getting Started with Hibernate](https://docs.hibernate.org/orm/6.4/quickstart/html_single/#hibernate-gsg-tutorial-annotations-test)

<details>
<summary><b>💡 Вот так 💡</b></summary>

```java
ServiceRegistry serviceRegistry = new StandardServiceRegistryBuilder()
        .build();

sessionFactory = new MetadataSources(serviceRegistry)
        .addAnnotatedClasses(Player.class, Match.class)
        .buildMetadata()
        .buildSessionFactory();
```

При таком подходе, настройки из `src/main/resources/hibernate.properties` подтянутся без явного указания пути к этому файлу.

</details>

<details>
<summary><b>💡 Со всеми предложенными изменениями HibernateUtil может выглядеть так 💡</b></summary>

```java
public class HibernateUtil {
    private HibernateUtil() {
    }

    public static SessionFactory getSessionFactory() {
        return Holder.SESSION_FACTORY;
    }

    private static SessionFactory buildSessionFactory() {
        try {
            ServiceRegistry serviceRegistry = new StandardServiceRegistryBuilder()
                    .build();

            return new MetadataSources(serviceRegistry)
                    .addAnnotatedClasses(Player.class, Match.class)
                    .buildMetadata()
                    .buildSessionFactory();

        } catch (Exception e) {
            throw new DataBaseException("Error initializing Hibernate: " + e.getMessage());
        }
    }

    public static void shutdown() {
        try {
            Holder.SESSION_FACTORY.close();
        } catch (Exception ignored) {
        }
    }

    private static class Holder {
        private static final SessionFactory SESSION_FACTORY = buildSessionFactory();
    }
}
```

</details>

- создание SessionFactory можно вынести в listener, чтобы оно происходило на старте приложения (eager), а не при первом обращении к БД

<details>
<summary><b>💡 Вот пример такого класса 💡</b></summary>

```java
@WebListener
public class AppStartupListener implements ServletContextListener {
    @Override
    public void contextInitialized(ServletContextEvent sce) {
        try {
            HibernateUtil.getSessionFactory();
        } catch (Exception e) {
            throw new RuntimeException("Application startup failed", e);
        }
    }

    @Override
    public void contextDestroyed(ServletContextEvent sce) {
        HibernateUtil.shutdown();
    }
}
```

</details>

### UUIDUtil
- класс можно полностью упразднить без потери качества кода: сейчас он служит просто обёрткой над статическими методами класса java.util.UUID, не добавляя к ним никакого поведения. Там, где сейчас используется UUIDUtil, можно напрямую вызывать нужные методы UUID. 

### Validator
- аннотация `@NoArgsConstructor` не нужна в этом классе, так как в java, если в классе не объявлен другой конструктор, то публичный конструктор без параметров будет по умолчанию
- метод `validate()` выполняет сразу три проверки и будет лучше читаться, если разделить его на три разных метода:
```java
public static void validate(String name) {
    checkIsEmpty(name);
    checkLength(name);
    checkConsistsOnlyValidCharacters(name);
}

private static void checkIsEmpty(String name) {
    if (name == null || name.isBlank()) {
        throw new BadRequestException(MISSING_PARAMETER_MESSAGE + name);
    }
}

private static void checkLength(String name) {
    if (name.length() < 2 || name.length() > 30) {
        throw new BadRequestException(INVALID_NAME_LENGTH_MESSAGE);
    }
}

private static void checkConsistsOnlyValidCharacters(String name) {
    if (!SYMBOL_VALIDATION_PATTERN.matcher(name).matches()){
        throw new BadRequestException(INVALID_FORMAT_NAME_MESSAGE);
    }
}
```
- чтобы одним вызовом из клиентского кода выполнить все проверки можно продолжить рефакторинг до
```java
// может принимать DTO состоящий из двух имён
public static void validateTwoNames(String firstName, String secondName) {
    validateName(firstName);
    validateName(secondName);
    checkUnique(firstName, secondName);
}

// ... — другие уже описанные методы (только сделать их private)

private static void checkUnique(String firstName, String secondName) {
    if (firstName.equals(secondName)) { // или equalsIgnoreCase()
        throw new DuplicateNameException(DUPLICATE_NAME_MESSAGE);
    }
}
```
- значения минимальной и максимальной длины имени можно вынести в константы
- можно перейти на валидацию с помощью Hibernate Validator
- для ошибок валидации лучше создать отдельное исключение, например ValidationException. Валидатор не должен выбрасывать HTTP-ориентированные исключения (BadRequestException)
- ❗️сейчас правила валидации слишком строгие без понятных причин для этого. Имеет смысл разрешить использование в имени пробела, дефиса и апострофа

## JSP
- ❗️JSP страницы должны быть в папке `WEB-INF`, чтобы к ним не было прямого доступа. Файлы из этой папки нельзя запросить напрямую, только через котроллеры.
- после реализации классов DTO (в соответствии с Single Responsibility Principle, SRP) в файлах JSP потребуются соответствующие измененния в местах обращения к полям DTO 
- сейчас в `match-score.jsp` так — `${pageContext.request.contextPath}/css/style.css`, а в других — просто `css/style.css`. Чтобы избежать ошибок при деплое по нестандартному context path, стоит везде использовать `${pageContext.request.contextPath}` 
- css стили можно выносить в отдельный файл для устранения дублирования и более удобного чтения JSP
- ❗️в JSP не должно быть логики определения победителя (JavaScript в `match-score.jsp`) — его нужно устанавливать на этапе маппинга Entity -> DTO 
- в `index.jsp` пропущена открываящая кавычка в строке 12: `<script src=${pageContext.request.contextPath}/js/app.js"></script>` -> `<script src="${pageContext.request.contextPath}/js/app.js"></script>` 
- для ссылок вида `... href="page_name ...` стоит использовать тег `<c:url>` для корректной кодировки URL параметров, а также для повышения безопасности при генерации ссылок (например, при фильтрации по имени игрока)
- ❗️в пагинации на странице `matches.jsp` отображаются все страницы, что будет плохо выглядеть при большом количестве страниц. Лучше сделать отображение текущей и +- 2 страниц вокруг неё.

## Test
### MatchScoreCalculationServiceTest
- ❗️тесты `shouldEndMatchAfterWinningTwoSets()` и `shouldWinSetWhenWinningSeventhGame()` проверяют выигрыш при 7 очках в сете — стоит переписать в соответствии с правилами тенниса 
- в тестах тоже лучше избегать магических переменных
- при переходе на ООП реализацию логики подсчёта очков, стоит добавить больше тестов (особенно для нетиповых ситуаций, вроде режима deuce-advantage)

## В целом по проекту
- местами в некоторых классах немного не хватает форматирования. Перед `git commit` можно нажимать (`cmd + alt + l` в Idea на mac os)
- ❗️исключения от Hibernate и БД (и некоторые другие) нигде не обрабатываются, поэтому при любой ошибке во время работы репозитория пользователь видит неинформативную страницу от томката. Следует использовать специализированные исключения, чтобы оборачивать в них возникающие ошибки и убедиться, что они обрабатываются в фильтре, чтобы улучшить пользовательский опыт, доступно информируя о том, что пошло не так.
- во многих классах есть аннотации, которые не используются — стоит проверить их необходимость и удалить лишние, чтобы не генерировать в проекте неиспользуемый код
- ❗️В проекте отсутствуют интерфейсы для ключевых компонентов (репозитории и сервисы), что приводит к нарушению принципа инверсии зависимостей (Dependency Inversion). Было бы правильным — выделить интерфейсы для сервисов (возможно) и репозиториев и внедрять зависимости через них.
- ❗️объекты репозиториев и некоторых сервисов создаются внутри класса, в месте объявления. Это делает невозможным подмену реализации и нарушает принцип открытости/закрытости (Open/Closed Principle) — класс должен быть открыт для расширения, но закрыт для изменений. Также классы, зависящие от конкретной реализации репозиториев невозможно протестировать в отрыве от БД.
  Вместо этого лучше, чтобы сервис зависел от абстракции (интерфейса). Тогда можно будет легко подменять реализацию, не меняя код сервиса.
  Объекты сервисов и репозиториев могут быть синглтонами или просто создаваться один раз при старте приложения и помещаться, к примеру, в контекст. А в нужном месте их можно получать вместо того, чтобы ещё раз создавать заново.

## Другое
- имена пакетов в java принято называть в стиле `имя` (lowercase). Сейчас имя корневого пакета — `Java`
- чтобы визуально протестировать пагинацию на странице списка матчей надо вручную завести много матчей. И ещё больше — чтобы протестировать пагинацию при фильтрации по имени игрока. Поэтому было бы хорошо добавлять при старте приложения (или деплое) нужное количество матчей в БД. В текущей реализации проще всего это сделать просто продублировав несколько раз строки в `init_database.sql`  
- ❗️учётные данные из `hibernate.properties`
```properties
hibernate.connection.username=sa
hibernate.connection.password=
```
не должны попадать в github

## Плюсы
- имена классов, методов и переменных в основном понятны и отражают их назначение
- хорошее использование констант
- довольно хорошо читаемое разделение кода на методы (даже там, где не соблюдается SRP)
- везде где нужно, правильно используются контекстные пути
- использование DTO (хоть их и стоит ещё доработать)
- разделение на слои (Controller -> Service -> Repository)
- репозитории инкапсулируют работу с БД
- использование Lombok для уменьшения boilerplate-кода
- использование транзакций при работе с БД
- хорошая работа с сессиями
- логика проверки имён вынесена в отдельный валидатор
- использование MapStruct
- обработка исключений через фильтр
- есть фильтрация матчей по имени игрока (+ попытка реализовать поиск по части имени)
- наличие тестов
- успешный деплой приложения
- добавлены тестовые данные в БД при деплое
- реализована пагинация на странице поиска матчей
- работающая бизнес-логика
- отдельная страница для отображения победителя в матче

## Может быть полезным
- Посмотреть, что такое ServletContextListener
- Посмотреть, что такое Hibernate Validator
- Посмотреть, записи стримов по декомпозиции в ООП (наиболее полезным для этого проекта может оказаться стрим по игре Орёл и решка)
- Посмотреть лучшие практики использования DTO
- Посмотреть, что такое Проблема N+1 в запросах к БД и способы её устранения
- Повторить и разобрать на примерах принципы SOLID (для начала DIP)
- Для удобного запуска проекта из Idea CE во время разработки можно настроить плагины Smart Tomcat или Jetty (для Maven проекта)/Gretty (для Gradle)

## Заключение
Реализация проекта демонстрирует хорошее понимание разработки на Java и навык построения веб-приложений.
Мало замечаний, которые можно отнести к критическим, что позволяет доработать эти части относительно небольшими усилиями.
Архитектура проекта достаточно хорошо продумана и разделена на слои, поэтому рефакторинг и доработку можно выполнить довольно легко.
