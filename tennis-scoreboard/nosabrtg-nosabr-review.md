# Review на реализацию от [@nosabrtg](https://github.com/nosabr/tennis-score) проекта [Табло теннисного матча](https://zhukovsd.github.io/java-backend-learning-course/projects/tennis-scoreboard/)

```text
1. Знаком ❗️ помечены критически важные замечания, а также места нарушения ТЗ.
2. Если ❗️ стоит перед заголовком, значит он относится ко всем пунктам этого раздела.
3. Замечания, указанные в пункте с именем пакета, относятся ко всем классам этого пакета или ко всем классам этого слоя.
4. Знаком 💡 помечены блоки, в которых содержится подсказка по реализации какого-то приёма или части кода. 
   Такие пункты всегда находятся в сворачиваемом блоке и разворачиваются по нажатию. 
   Перед их раскрытием стоит постараться придумать или поискать решение самостоятельно. 
```

## Функциональный обзор
- ❗️когда счёт в сете становится `40:40` и начинается "режим Advantage", то на табло очки обнуляются и дальше показываются не `40:AD -> 40:40 -> AD:40`, а `1:0 -> 0:0 -> 0:1`. Сам подсчёт происходит правильно, но такое визуальное отображение сбивает с толку
- чтобы уйти со страницы со списком сыгранных матчей надо нажать на маленькую надпись `Home`. В то время как с домашней страницы на другие переход происходит через большие кнопки. Можно и на страницу с матчами добавить явную кнопку "домой" или повесить эту ссылку на заголовок в хэдере — это сделает навигацию более интуитивной  

## JSP
- ❗️JSP страницы должны быть в папке `WEB-INF`, чтобы к ним не было прямого доступа. Файлы из этой папки нельзя запросить напрямую, только через сервлеты.
- можно добавить страницу `error.jsp` которая будет в едином стиле проекта сообщать пользователю об ошибках в случае необходимости
- css стили можно выносить в отдельный файл для устранения дублирования и более удобного чтения JSP
- Параметры URL, а также которые приходят от пользователя лучше экранировать — выводить через тег `<c:out>`. `<c:out>` — это JSTL (JavaServer Pages Standard Tag Library) тег, который автоматически экранирует специальные HTML символы. На `match.jsp` и `matches.jsp` уже подключен JSTL Core (строка `<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>` в начале страницы)
- страница `on-going-match.jsp` не используется в проекте — её можно удалить

## Models
- сейчас очки в гейме хранятся в классе Score, очки в сете — в классе Pair, а очки в матче снова в классе Score. Лучшим решением в этом направлении было бы, чтобы за счёт на каждом уровне отвечал отдельный класс. Такой подход больше соответствовал бы принципу единственной ответственности для каждого класса.
- также сейчас классы, хранящие счёт, имеют признаки "анемичной модели" — они являются лишь контейнерами для данных, а вся логика находится в сервисных классах. Если бы у классов вместо простых сеттеров были методы, совершающие необходимую работу над полями класса, это больше соответствовало бы ООП стилю и обязанности класса. Также, эту часть логики было бы легче тестировать.
<details>
<summary><b>💡 Вот упрощённый пример такого класса 💡</b></summary>

```java
public class GameScore {
    private static final Point INITIAL_POINTS = Point.LOVE;
    private Point firstPlayerPoints;
    private Point secondPlayerPoints;

    public GameScore() {
        this.firstPlayerPoints = INITIAL_POINTS;
        this.secondPlayerPoints = INITIAL_POINTS;
    }

    public Point getFirstPlayerPoints() {
        return firstPlayerPoints;
    }

    public void pointWonByFirstPlayer() {
        firstPlayerPoints = firstPlayerPoints.next();
    }

    public void loseAdvantageByFirstPlayer() {
        if (firstPlayerPoints != Point.ADVANTAGE) {
            throw new IllegalStateException("First player has no advantage.");
        }
        firstPlayerPoints = Point.FORTY;
    }

    public boolean hasGameWinner() {
        return isFirstPlayerWinner() || isSecondPlayerWinner();
    }

    public boolean isFirstPlayerWinner() {
        // логика определения победы первого игрока
    }

    public boolean isSecondPlayerWinner() {
        // логика определения победы второго игрока
    }
}

enum Point {
    LOVE,
    FIFTEEN,
    THIRTY,
    FORTY,
    ADVANTAGE;

    public Point next() {
        if (this == ADVANTAGE) {
            throw new IllegalStateException("Has no points after advantage.");
        }
        return values()[ordinal() + 1];
    }
}
```

При таком подходе от сеттеров стоит избавиться, чтобы состояние счёта управлялось только явно предусмотренными для этого методами. 

</details>

- недостатки архитектуры доменных моделей оказывают каскадное влияние на слой сервисов

### scoreUtil
- имена пакетов, состоящие из нескольких слов, [в java принято называть](https://docs.oracle.com/javase/tutorial/java/package/namingpkgs.html) в стиле `имяпакета` (слитно и в lowercase) и иногда (если имя пишется через дефис) в стиле `мало_помалу` (snake case).   
- пакет называется `scoreUtil`, лежащий в нём класс Pair не является утилитным, поэтому стоит перенести его к другим моделям

### Pair
- ❗️отсутствует модификатор private у полей класса. С помощью аннотации `@Data` генерируются публичные сеттеры и геттеры и это нормально, но сейчас поля класса и без них доступны к чтению и изменению внутри пакета — так быть не должно. Хотя сейчас в этом пакете больше нет классов, отсутствие модификаторов private нарушает инкапсуляцию, так как позволяет изменять состояние объекта извне без контроля. 
- раз используешь Lombok, то можно добавить аннотацию `@AllArgsConstructor` и конструктор с параметрами для всех полей тоже сгенерируется
- название Pair слишком абстрактное и не отражает предметную область — счёт в теннисе
- также не является очевидным, что означают first и second. В теннисе, насколько мне известно, есть вариант игры пара на пару (два игрока против двух игроков на одном корте), из-за этого класс можно принять за отображение такой пары. Но в этом проекте это не так, поэтому стоит дать классу более понятное название, например `SetScore`, а полям `firstPlayerGames` и `secondPlayerGames`.
- в классе Score в методе `fillSets()` создаётся объект класса Pair со стартовыми параметрами для переменных first и second: `sets[i] = new Pair(0, 0);`. Чтобы в клиентском коде было меньше магических (т.е. не имеющих понятное название и происхождение) чисел, можно в классе `Pair` создать конструктор без параметров, который при вызове будет инициализировать поля класса стартовыми значениями. В текущем варианте класса это будет работать и простым добавлением аннотации `@NoArgsConstructor` к классу Pair, потому что переменные типа int по умолчанию инициализируются 0, но можно сделать это и более явным, создав конструктор вручную. 

### Score
- ❗️у поля `Pair[] sets` пропущен модификатор private
- сейчас поле `Pair[] sets` является массивом со строго заданным размером, поэтому для отслеживания номера сета используется дополнительная переменная `int currentSet`. Также объектов, содержащих счёт сета, всегда 3, даже если матч закончился после второго сета. Это создаёт невынужденное место для возникновения ошибок. Для хранения объектов счёта в сете больше подошёл бы, например, ArrayList. Тогда объектов было бы ровно столько, сколько фактически сыграно сетов, а также отпала бы необходимость в дополнительной переменной — текущий сет всегда был бы последним в списке.
- ❗️поле `int gameMode` объявлено как int, но фактически имеет только 3 значения, которые кодируют разные состояния матча. Для этой цели лучше подойдёт перечисление, например такое:
```java
public enum GameMode {
    REGULAR,
    ADVANTAGE,
    TIEBREAK;
}
```
Использование чисел вместо перечислений делает код менее читаемым и более подверженным ошибкам
- поле `int currentSet` инициализируется дважды — в месте объявления и в методе `fillSets()`
- ❗️поле `int currentSet` является указателем на индекс текущего сета в массиве `Pair[] sets`. При этом у него есть публичный сеттер, через который значение индекса меняется за пределами этого класса. Это бесконечный источник потенциальных ошибок ArrayIndexOutOfBoundsException. Если в классе есть массив и по какой-то причине нужно отдельно хранить его индекс, то любое изменение этого индекса должно быть инкапсулировано внутри этого класса. Максимум, что можно показать наружу — это публичный метод с продуманной логикой и говорящим названием, который будет изменять индекс в допустимых пределах.

### Player и Match
- `@EqualsAndHashCode` (которая входит в `@Data`) обычно не используют в jpa сущностях. Конкретно здесь проблем быть не должно, но использование этих аннотаций не является хорошей практикой.
- если имя поля совпадает с названием колонки, то параметр name в аннотации `@Column` можно опустить
- имя таблицы с матчами написано в верхнем регистре `@Table(name = "MATCHES")`, тогда как имя таблицы с игроками в нижнем `@Table(name = "players")` — стоит выбрать что-то одно.

### Player
- лишние скобки вокруг `name` в аннотации `@Column`:

  `@Column(name = ("name"),nullable = false, unique = true)` -> `@Column(name = "name", nullable = false, unique = true)` 

### Match
- аннотации удобнее читать, когда они расположены одна под другой, а не в одну строку.
Сейчас так:
```java
@ManyToOne @JoinColumn(name = "player1_id", nullable = false)
private Player player1;
```
Лучше так:
```java
@ManyToOne 
@JoinColumn(name = "player1_id", nullable = false)
private Player player1;
```
- ❗️у поля `boolean isMatchFinished` пропущен модификатор private
- названия переменных принято делать существительными (а у boolean часто и прилагательными): `isMatchFinished` — глагольная конструкция, `finishedMatch` — прилагательное + существительное. Ещё лучше будет просто `finished` — без суффикса/префикса `Match`, потому что `finishedMatch` больше подошло бы объекту матча, который завершен, а также из-за избыточности контекста — переменная и так находится в классе Match. Также лучше избегать префикса `is` в названии переменной, так как он обычно используется в геттере для boolean полей. Сейчас сгенерированный Lombok геттер тоже называется `isMatchFinished()`:
```java
@Generated
public boolean isMatchFinished() {
    return this.isMatchFinished;
}
```
Если метод и поле имеют одинаковое имя, это усложняет чтение и поддержку кода и делает возможными ошибки вызова.

- в классе происходит смешение ответственности — он пытается быть одновременно и сущностью БД, и контейнером для текущего состояния матча (включая хранение счета). Лучшим решением будет разделить его на два класса: Match — сущность БД, содержащая только постоянные данные (игроки, победитель) и OngoingMatch — текущий матч (в процессе), с состоянием счета и логикой его изменения.
- Использование аннотации Lombok `@EqualsAndHashCode` (которая входит в `@Data`) в классе с полями, помеченными `@Transient` делает фактическую работу метода equals отличной от ожиданий по контракту.
<details>
<summary><b>вот по какой причине</b></summary>

Несмотря на то, что [Project Lombok пишет, что при генерации метода equals "по умолчанию будут использоваться все non-static, non-transient поля"](https://projectlombok.org/features/EqualsAndHashCode), поля
```java
@Transient
private Score score = new Score();

@Transient
boolean isMatchFinished = false;
```

тоже будут использованы для сравнения объектов. В чём можно убедиться, посмотрев сгенерированный код в `target/classes/com/example/tennisscoreboard2025/models/Match.class` после компиляции (`mvn compile`)

<details>
<summary><b>сгенерированные equals() и hashcode() выглядят так</b></summary>

```java
@Generated
public boolean equals(Object o) {
    if (o == this) {
        return true;
    } else if (!(o instanceof Match)) {
        return false;
    } else {
        Match other = (Match)o;
        if (!other.canEqual(this)) {
            return false;
        } else if (this.isMatchFinished() != other.isMatchFinished()) { // здесь используется поле, помеченное @Transient
            return false;
        } else {
            label73: {
                Object this$id = this.getId();
                Object other$id = other.getId();
                if (this$id == null) {
                    if (other$id == null) {
                        break label73;
                    }
                } else if (this$id.equals(other$id)) {
                    break label73;
                }

                return false;
            }

            Object this$player1 = this.getPlayer1();
            Object other$player1 = other.getPlayer1();
            if (this$player1 == null) {
                if (other$player1 != null) {
                    return false;
                }
            } else if (!this$player1.equals(other$player1)) {
                return false;
            }

            label59: {
                Object this$player2 = this.getPlayer2();
                Object other$player2 = other.getPlayer2();
                if (this$player2 == null) {
                    if (other$player2 == null) {
                        break label59;
                    }
                } else if (this$player2.equals(other$player2)) {
                    break label59;
                }

                return false;
            }

            Object this$winner = this.getWinner();
            Object other$winner = other.getWinner();
            if (this$winner == null) {
                if (other$winner != null) {
                    return false;
                }
            } else if (!this$winner.equals(other$winner)) {
                return false;
            }

            // здесь используется поле, помеченное @Transient
            Object this$score = this.getScore();
            Object other$score = other.getScore();
            if (this$score == null) {
                if (other$score != null) {
                    return false;
                }
            } else if (!this$score.equals(other$score)) {
                return false;
            }

            return true;
        }
    }
}

@Generated
protected boolean canEqual(Object other) {
    return other instanceof Match;
}

@Generated
public int hashCode() {
    int PRIME = true;
    int result = 1;
    result = result * 59 + (this.isMatchFinished() ? 79 : 97); // здесь используется поле, помеченное @Transient
    Object $id = this.getId();
    result = result * 59 + ($id == null ? 43 : $id.hashCode());
    Object $player1 = this.getPlayer1();
    result = result * 59 + ($player1 == null ? 43 : $player1.hashCode());
    Object $player2 = this.getPlayer2();
    result = result * 59 + ($player2 == null ? 43 : $player2.hashCode());
    Object $winner = this.getWinner();
    result = result * 59 + ($winner == null ? 43 : $winner.hashCode());
    Object $score = this.getScore();
    result = result * 59 + ($score == null ? 43 : $score.hashCode()); // здесь используется поле, помеченное @Transient
    return result;
}
```

</details>

Поэтому два объекта Match при изменении счёта (поля Score) будут считаться различными:

<details>
<summary><b>как в этом примере</b></summary>

```java
public static void main(String[] args) {
    Match match1 = new Match();
    Match match2 = new Match();

    // Если transient-поля разные, equals() вернёт false
    match1.getScore().setFirstPlayerPoints(15);
    match2.getScore().setFirstPlayerPoints(30);

    System.out.println(match1.equals(match2)); // false
}
```

</details>

И хотя в конкретной реализации проекта с этим не должно быть проблем, это не соответствует логике в текущем проекте, а также ожидаемому поведению от equals-hashcode.

</details>

## DAO
- JPQL запросы можно вынести в константы и дать им понятные имена, это сделает код методов ещё более читаемым
- текст запроса удобнее читать, когда он логично разбит на строки:

```sqlite-sql
SELECT p 
FROM Player p 
WHERE p.name = :name
```
- В текущей реализации методы сохранения сущностей `save(Player p)` и `save(Match match)` возвращают void. Это допустимо, но обычно в таких методах возвращают сохранённую сущность (особенно если у неё генерируется ID) или, по крайней мере, тот же объект. Это позволяет получить сгенерированный идентификатор более явно.
- ❗️В методах сохранения сущностей `save(Player p)` и `save(Match match)` не происходит отката транзакции, при ошибке. Это может привести к неконсистентности данных и утечке ресурсов (например, блокировкам в БД). Транзакция останется открытой, и соединение может не вернуться в пул. Стоит добавить блок catch, в котором вызывать `tx.rollback()`.
- сейчас в каждом методе используется `openSession()`: 

  `Session session = HibernateSessionFactoryUtil.getSessionFactory().openSession();` — при таком подходе каждый раз (в каждом методе) создаётся новый объект Session. 

  Вместо этого можно использовать `getCurrentSession()` — этот метод возвращает сессию текущего контекста. Так в одном потоке (HTTP-запросе) будет использоваться одна и та же сессия. В этом случае закрытие сессии тоже будет происходить автоматически.
  Для получения сессии через `getCurrentSession()` надо добавить в `hibernate.cfg.xml` свойство `hibernate.current_session_context_class`. 

<details>
<summary><b>💡 Например, так (для режима одна-сессия-на-поток) 💡</b></summary>

```xml
<property name="hibernate.current_session_context_class">thread</property>
```

</details>

<details>
<summary><b>💡 Или так в HibernateSessionFactoryUtil 💡</b></summary>

В статическом блоке, перед созданием SessionFactory
```java
if (!cfg.getProperties().containsKey("hibernate.current_session_context_class")) {
    cfg.setProperty("hibernate.current_session_context_class", "thread");
}
```

</details>

- Сейчас транзакции управляются на уровне DAO. Можно перенести это на сервисный слой, чтобы охватывать несколько операций с DAO в одной транзакции. В текущем коде, если сервис использует несколько DAO методов, каждый будет в своей транзакции, что может привести к неконсистентности.
- в обоих классах DAO есть дублирующиеся части кода и даже методы, отличающиеся только типом сущности, с которой они работают. Чтобы этого избежать стоит сделать базовый DAO класс и вынести общую логику в него.
- ❗️В текущем коде DAO классов исключения не обрабатываются. Это означает, что они будут прокидываться на уровень выше. В целом, это допустимо, если на уровне сервиса или, например, в фильтре есть обработка исключений (чего в нет в этой реализации — об этом будет сказано ниже). Однако, в DAO следует убедиться, что ресурсы (сессии) закрываются даже при исключении. В текущем коде сессии не закрываются (закомментирован вызов session.close() и не используется try-with-resources), что приводит к утечкам. Поэтому необходимо использовать try-with-resources или блок finally для закрытия сессии.
- в текущей реализации DAO классы не имеют интерфейсов. Внедрение интерфейсов позволило бы легче подменять реализации, например, для тестирования (моки) или для будущих изменений.
- ❗️проблема N+1 в запросах к БД, что излишне нагружает БД и снижает производительность при больших объёмах данных

### PlayerDAO
- метод `Optional<Player> findById(Long id)` нигде не используется в проекте и скорее всего никогда не будет использоваться, а значит его можно удалить
- в методе `Optional<Player> findByName(String name)` результат получается через создание списка, а затем ещё и стрима:
```java
List<Player> players = session
        .createQuery(hql, Player.class)
        .setParameter("name", name)
        .getResultList();
...
return players.stream().findFirst();
```

это лишнее создание объектов и его стоит заменить на
```java
Optional<Player> optionalPlayer = session
            .createQuery(hql, Player.class)
            .setParameter("name", name)
            .uniqueResultOptional();
...
return optionalPlayer;
```
поскольку имя игрока уникально в БД
- ❗️Сессии закрываются вручную в каждом методе, поэтому если между `openSession()` и `close()` произойдет исключение, сессия не закроется. Это приведёт к утечке ресурсов соединения с базой данных. Сессия Hibernate оборачивает JDBC-соединение, которое является ограниченным ресурсом. Если такие утечки будут происходить часто, то пул соединений может исчерпаться, что приведёт к невозможности установления новых соединений с БД и, как следствие, к падению приложения. Session реализует интерфейс AutoCloseable, поэтому стоит использовать её в try-with-resources для автоматического закрытия даже в случае ошибки.
- В каждом методе повторяется логика работы с сессией — можно подумать, как этого избежать
<details>
<summary><b>💡 Например так 💡 </b></summary>

Создать в базовом классе DAO два метода и вызывать их в необходимых местах
```java
// Для операций чтения (возвращающих результат)
protected <T> T executeQuery(Function<Session, T> function) {
    try (Session session = HibernateSessionFactoryUtil.getSessionFactory().openSession()) {
        return function.apply(session);
    } catch (Exception e) {
        throw new RuntimeException("Query execution failed", e);
    }
}

// Для операций записи (изменяющих данные)
protected void executeTransaction(Consumer<Session> operation) {
    try (Session session = HibernateSessionFactoryUtil.getSessionFactory().openSession()) {
        Transaction tx = session.beginTransaction();
        try {
            operation.accept(session);
            tx.commit();
        } catch (Exception e) {
            tx.rollback();
            throw new RuntimeException("Transaction failed", e);
        }
    }
}
```

Только лучше использовать специализированные исключения вместо RuntimeException 

</details>

### MatchDAO
- ключевые слова в JPQL запросе в методе `int count()` написаны в нижнем регистре (lowercase) `"select count(m) from Match m"` — лучше, по аналогии со всем другими запросами, изменить их регистр на верхний (uppercase). Это не влияет на корректность работы, так как JPQL не чувствителен к регистру, но принято использовать верхний регистр для ключевых слов.

  В Idea для изменения регистра всего слова (на котором находится курсор) есть удобное сочетание `cmd + shift + u` (на mac os).
- метод `List<Match> findAll()` нигде не используется в проекте и скорее всего никогда не будет использоваться, а значит его можно удалить
- ❗️во всех методах используется `openSession()` для создания сессии: `Session session = HibernateSessionFactoryUtil.getSessionFactory().openSession();`. Однако вызов `//session.close();`, необходимый при таком подходе, почти во всех методах закомментирован. Это приводит к уже описанным выше проблемам. Стоит использовать сессию в try-with-resources или перейти на `getCurrentSession()`.
- ❗️В методах поиска загружаются сущности Match, которые связаны с Player отношением многие-к-одному. В текущей реализации при загрузке одного матча будет выполняться ещё два запроса для загрузки связанных с ним игроков (проблема N+1). Следует это исправить — например, изменить запросы так, чтобы сразу загружать связанные сущности.

<details>
<summary><b>💡 Вот пример такого запроса 💡</b></summary>

Оптимизируем запросы с помощью JOIN FETCH
```java
String FIND_BY_NAME_JPQL = """
        SELECT m FROM Match m
        LEFT JOIN FETCH m.player1
        LEFT JOIN FETCH m.player2
        LEFT JOIN FETCH m.winner
        WHERE m.player1.name LIKE :name OR m.player2.name LIKE :name
        """;
```

</details>

- метод в DAO не должен вычислять смещение: `query.setFirstResult((page - 1) * pageSize);` в методе `List<Match> findAll(int page, int pageSize)` — это ближе к ответственности слоя сервисов. Логика, будет чище, если он будет принимать уже готовые значения и просто подставлять их в запрос:

```java
public List<Match> findAll(int offset, int limit) {
    ...
    query.setFirstResult(offset);
    query.setMaxResults(limit);
    ...
}
```
- метод `Optional<Match> findByName(String name)` используется только в тестах — это слабая причина для его существования в DAO классе. Лучше переписать тесты без него и удалить этот метод.

- в методе `int countByName(String name)` используется вызов `Query createQuery(String queryString);`, который помечен `@Deprecated` и `@SuppressWarnings("rawtypes")`. Вместо него лучше использовать `<R> Query<R> createQuery(String queryString, Class<R> resultClass);` с более безопасным приведением типа:

  `Query<Long> query = session.createQuery(COUNT_BY_NAME_JPQL);` -> `Query<Long> query = session.createQuery(COUNT_BY_NAME_JPQL, Long.class);`

- результат от `createQuery()` сохраняется в переменную типа `Query<R>` — этот интерфейс помечен `@Incubating`, а значит его API может измениться в будущих версиях Hibernate. И хотя это никак не повлияет на работу этого проекта, для production кода лучше использовать стабильный `TypedQuery<X>`:

  `Query<Long> query = session.createQuery(COUNT_BY_NAME_JPQL);` -> `TypedQuery<Long> query = session.createQuery(COUNT_BY_NAME_JPQL, Long.class);`

- можно добавить независимость от регистра при поиске по имени на уровне БД. 
<details>
<summary><b>💡 Например, так 💡</b></summary>

Используем функцию LOWER() и передаём имя в нижнем регистре
```java
String FIND_BY_NAME_JPQL = """
        FROM Match m 
        WHERE LOWER(m.player1.name) LIKE :name 
            OR LOWER(m.player2.name) LIKE :name
        ORDER BY m.id ASC
        """;

query.setParameter("name", "%" + name.toLowerCase() + "%");
```

Тогда, для оптимальной производительности при больших объемах данных, пришлось бы подумать о сохранении преимущества от индекса по имени в БД.

</details>

## Services
- Жёсткая связь через `new` (объекты DAO и других сервисов создаются в классах) ограничивает гибкость и тестируемость кода. Внедрение зависимостей через конструктор — это способ устранить эти недостатки.
- ❗️Отсутствует обработка исключений от DAO слоя. Если, например, при сохранении игрока произойдет ошибка, то исключение будет проброшено выше. Это может быть приемлемо, если на уровне выше будет обработка, но в текущей реализации её нет. Следует добавить обработку исключений в сервисах или фильтре.

### MatchGenerationService
- В коде нет явного управления транзакциями. Метод `generateNewMatch()` вызывает два раза `getOrSavePlayer`, который выполняет запросы к БД (`findByName()` и `save()`). Если один из вызовов `getOrSavePlayer()` завершится успешно, а второй нет, то получится несогласованное состояние (один игрок будет создан, а второй нет). И хотя конкретно здесь из-за этого не должно быть проблем, лучше, чтобы весь метод `generateNewMatch()` выполнялся в одной транзакции или использовать другой подход к организации этой логики. 
- ❗️Race condition при создании игрока — сначала пытаемся найти игрока по имени и, если его нет, делаем ещё один запрос, чтобы сохранить:

```java
return playerDAO.findByName(firstPlayerName)
        .orElseGet(() -> {
            Player newPlayer = new Player();
            newPlayer.setName(firstPlayerName);
            playerDAO.save(newPlayer);
            return newPlayer;
        });
```

Между поиском и сохранением игрока другой поток может создать игрока с тем же именем. И тогда при сохранении возникнет исключение, которе нигде не обрабатывается пользователь увидит страницу с кодом 500. Хотя создать матч с уже существующим в БД игроком — нормальная ситуация. Вместо этого надо сразу пытаться сохранить игрока, позволить БД выбросить ошибку, если такой игрок уже существует и тогда уже обработать этот случай, продолжив работу приложения.
Кроме спасения от race condition, это уменьшит количество запросов, а значит снизит нагрузку на БД.

- имя параметра в методе `Player getOrSavePlayer(String firstPlayerName)` должно быть `String name`. Префикс `first` не нужен потому, что метод работает с обоими игроками, а префикс `Player` лишний так как он уже содержится в названии метода, а также этот контекст понятен из возвращаемого значения. 
- можно рассмотреть переименование методов: 

  `Match generateNewMatch(String firstPlayerName, String secondPlayerName)` -> `Match create(String firstPlayerName, String secondPlayerName)`

  `Player getOrSavePlayer(String firstPlayerName)` -> `Player getOrCreate(String name)`
- можно сделать метод
```java
private Player getOrSavePlayer(String firstPlayerName){
    return playerDAO.findByName(firstPlayerName)
            .orElseGet(() -> {
                Player newPlayer = new Player();
                newPlayer.setName(firstPlayerName);
                playerDAO.save(newPlayer);
                return newPlayer;
            });
}
```
ещё более читаемым
```java
private Player getOrCreate(String name) {
    return playerDAO.findByName(name)
            .orElseGet(() -> create(name));
}

private Player create(String name) {
    Player player = new Player();
    player.setName(name);
    playerDAO.save(player);
    return player;
}
```

### MatchService
- из сигнатуры метода `void handlePost(String uuid, int scoredPlayerNumber)` не понятно, что именно он делает. Стоит дать ему более информативное название, объясняющее его работу. В текущей реализации ему сложно дать подходящее название, так как он отвечает за слишком многое.    
- метод `handlePost()` перегружен ответственностями: валидирует (косвенно)/парсит входные данные, занимается поиском сущности (через OngoingMatchService), проверяет бизнес-правила, изменяет счёт и управляет жизненным циклом матча. В таких случаях следует разделять метод на несколько специализированных методов — это сделает код более понятным, поддерживаемым и тестируемым.
- в методе `handlePost()` нет обработки исключения, в случае ошибки при `UUID.fromString(uuid)` 
- ❗️проверка `if (match.getScore().getCurrentSet() == 3 ...)` означает, что значение переменной `int currentSet` из класса Score принимает недопустимые значения, для массива размером `3` (т.к. индексация в массивах начинается с 0 -> `{0, 1, 2}`). Это происходит из-за возможности менять индекс за пределами класса Score (о чём уже сказано в соответствующем разделе). Использовать это в качестве обычной логики (при недопустимом значении не выбрасывается исключение) крайне плохая практика. Лучшим способом исправить ситуацию будет изменение архитектуры классов хранения/подсчёта очков. 
- логика завершения матча разделена между MatchService и FinishedMatchesPersistenceService
- Используется RuntimeException с сообщением `"no match in uuid: ..."`, что недостаточно информативно (и из-за слишком общего типа исключения, и из-за самого текста сообщения — лучше выбрать что-то без игры слов: match — совпадать, соответствовать и match — матч) и не является хорошей практикой. Лучше использовать специализированные исключения.
- метод `checkIfPlayerWin()`:

логику 
```java
if (condition) {
    return true;
} else {
    return false;
}
```
лучше заменить на
```java
return condition;
```
то есть
```java
if(score.getSets()[0].getFirst() <  score.getSets()[0].getSecond()
        && score.getSets()[1].getFirst() <  score.getSets()[1].getSecond()){
    return true;
}
return false;
```
на
```java
return score.getSets()[0].getFirst() < score.getSets()[0].getSecond()
        && score.getSets()[1].getFirst() < score.getSets()[1].getSecond();
```
вынесение условия проверки для каждого игрока в отдельный метод с говорящим названием ещё больше повысит читаемость.
- метод `checkIfPlayerWin()` проверяет не общую победу игрока, а только факт ранней победы в матче (в первых двух сетах), поэтому будет логично отразить это в его названии — например, `hasEarlyWin`.
- ❗️этот клас слишком много знает о том, как устроен класс Score, что создаёт длинные цепочки вызовов, повышает связанность кода и делает его сложнее в поддержке и рефакторинге. Решение — реализация методов для работы с собственными данными в классах доменной модели. 

### FinishedMatchesPersistenceService
- если для прохода по массиву используется индексированный цикл `for`, то верхней границей должна быть `array.length`. 
Сейчас в метод `findWinner()` знает о размере Pair[] sets
```java
Pair[] sets = match.getScore().getSets();
...
for (int i = 0; i < 3; i++) {
    ...    
}
```
лучше так
```java
Pair[] sets = match.getScore().getSets();
...
for (int i = 0; i < sets.length; i++) {
    ...    
}
```

- в методе `findWinner()` есть ошибка в логике подсчёта выигранных сетов

сейчас так (допускает, что при равном количестве очков победа засчитывается второму игроку)
```java
if(sets[i].getFirst() > sets[i].getSecond()){
    firstPlayerWinSets++;
} else secondPlayerWinSets++;
```
правильно так
```java
if (sets[i].getFirst() > sets[i].getSecond()) {
    firstPlayerWinSets++;
} else if (sets[i].getFirst() < sets[i].getSecond()) {
    secondPlayerWinSets++;
}
```
Даже если в этом проекте с таким поведением не возникнет проблем, класс сервиса не должен это предвидеть. И это не снимает с разработчика обязанность писать код, корректно обрабатывающий все инварианты. 
- тело блоков if-else для лучшей читаемости и предотвращения ошибок лучше всегда оборачивать в фигурные скобки

сейчас так
```java
if (sets[i].getFirst() > sets[i].getSecond()) {
    firstPlayerWinSets++;
} else secondPlayerWinSets++; 
```
лучше так (опускаю здесь повторное упоминание ошибке подсчёта)
```java
if (sets[i].getFirst() > sets[i].getSecond()) {
    firstPlayerWinSets++;
} else {
    secondPlayerWinSets++;
}
```
- если блок if содержит return, то else можно не писать

сейчас так (добавим фигурные скобки к исходому варианту для выразительности)
```java
if (firstPlayerWinSets > secondPlayerWinSets) {
    return match.getPlayer1();
} else {
    return match.getPlayer2();
}
```
лучше так
```java
if (firstPlayerWinSets > secondPlayerWinSets) {
    return match.getPlayer1();
}
return match.getPlayer2();
```
- ❗️в методе `findWinner()` не корректно обрабатывается вариант, когда количество очков у обоих игроков равно — сейчас в таком случае победителем считается второй игрок:
```java
if (firstPlayerWinSets > secondPlayerWinSets){
    return match.getPlayer1();
} else return match.getPlayer2();
```
- Используется RuntimeException с сообщением `"No match found with that uuid"`, что недостаточно информативно и не является хорошей практикой. Лучше использовать специализированные исключения.
- необъяснимо разные сообщения об ошибках в одинаковых ситуациях:
в классе MatchService
```java
if (matchOpt.isPresent()) {
    match = matchOpt.get();
} else {
    throw new RuntimeException("no match in uuid: " + uuid);
}
```
в классе FinishedMatchesPersistenceService
```java
if (matchOpt.isPresent()) {
    match = matchOpt.get();
} else {
    throw new RuntimeException("No match found with that uuid");
}
```
Лучше придерживаться одного формата.
- логика определения победителя не должна находиться в сервисе сохранения завершённых матчей. Более уместным будет реализовать её в классе текущего матча (то есть в OngoingMatch после проведённого рефакторинга/реимплементации моделей) 
- метод `endMatch()` будет уместно назвать `finishMatch`, поскольку в самой сущности Match используется именно этот глагол для описания завершения матча 
- в методе `endMatch()` нет обработки исключения, в случае ошибки при `UUID.fromString(uuid)`
- метод `endMatch()` (как и `handlePost` в MatchService) содержит больше одной ответстввенности. Его можно разделить на несколько более простых, каждый из которых будет иметь логичную порцию ответственности 

### OngoingMatchService
- не хватает фигурных скобок в блоке if в методе `getInstance()`
- данная реализация паттерна singleton не является потокобезопасной (из-за возможного race condition). Исправить это можно, добавив ключевое слово synchronized к объявлению метода `getInstance()` или реализовать паттерн с использованием статического вложенного класса (holder)
<details>
<summary><b>💡 Потокобезопасный вариант синглтона без synchronized 💡</b></summary>

```java
public class Singleton {
    // Приватный конструктор запрещает создание экземпляров вне класса
    private Singleton() {}

    // Статический вложенный класс — держатель единственного экземпляра Singleton
    private static class Holder {
        private static final Singleton INSTANCE = new Singleton();
    }

    // Метод для получения единственного экземпляра Singleton
    public static Singleton getInstance() {
        return Holder.INSTANCE;
    }
}
```
- Класс Holder загружается в память и инициализируется только тогда, когда метод `getInstance()` вызывается в первый раз. До этого момента экземпляр синглтона не создается, что позволяет сэкономить ресурсы, если синглтон не используется.
- JVM гарантирует, что инициализация статического поля (INSTANCE) в классе происходит потокобезопасным образом — только один раз одним потоком, и другие потоки не смогут получить доступ к полю до завершения его инициализации. Это устраняет необходимость в явной синхронизации.

</details>

### ScoreCalculationService
- ❗️при счёте 40 и добавлении очка значение становится 40 + 10 = 50. Хотя далее и проверяется, что если счёт стал больше 40, то происходит переход в режим 2 (Deuce/Advantage), значение счёта не должно принимать невалидные значения.
```java
private int getNextPointValue(int playerPoints, int gameMode) {
    if(gameMode == 1){
        if(playerPoints < 30){
            return playerPoints + 15;
        } else {
            return playerPoints + 10; // здесь счёт становится 50
        }
    } else if(gameMode == 2 || gameMode == 3){
        return playerPoints + 1;
    }
    return 0;
}
```

- ❗️Некорректная логика Deuce/Advantage: при счёте `40:40` очки сбрасываются и происходит переход в режим 2
```java
} else if(score.getFirstPlayerPoints() == 40 && score.getSecondPlayerPoints() == 40) {
    score.setGameMode(2); // Больше меньше
    resetPoints(score);
}
```
что приводит к некорректному отображению счёта: не `40:AD -> 40:40 -> AD:40`, а `1:0 -> 0:0 -> 0:1`.
Это находчивый приём унифицирующий вычисления, но для production кода следует написать логику, явно отражающую значения счёта в реальном теннисном матче. Правильная реализация будет лучше читаться, проще поддерживаться, а также точнее следовать ООП принципам и возможностям языка. 

- ❗️сервис знает слишком много о том, как устроен класс Score, полностью управляет его внутренним состоянием, что повышает связанность кода и делает его сложнее в поддержке и рефакторинге. Решение — реализация методов для работы с собственными данными в классах доменной модели. Также имеет смысл перейти на реализацию в ООП стиле — провести детальную декомпозицию предметной области, выделить соответствующие абстракции и наделить их нужным состоянием и поведением.
- пункт про магические переменные (из раздела **В целом по проекту**) в первую очередь относится к этому классу
- даже в текущем виде методы можно сделать более читаемыми: например, разделить на более мелкие, которые будут обрабатывать каждый свой режим игры
- благодаря осмысленным названиям переменных логику обработки очков можно проследить, но это было бы намного сложнее сделать разработчику, не знакомому с ТЗ. Стоит переписать эту часть проекта с учётом предложенных выше рекомендаций.

## Util
### HibernateSessionFactoryUtil
- ❗️не закрывается SessionFactory при остановке приложения. В веб-приложении обычно закрывают SessionFactory при остановке приложения. Это можно сделать, например, с помощью ServletContextListener.
- при переходе на автоматическое управление сессиями, можно будет добавить метод
```java
public static Session getCurrentSession() {
    return sessionFactory.getCurrentSession();
}
```
чтобы получение сессии не выглядело длинной цепочкой вызовов

## Servlets
- После перемещения JSP страниц в папку `WEB-INF` нужно будет поправить все пути к этим страницам в местах, где происходит forward

### MatchesServlet
- нет обработки NumberFormatException при `Integer.parseInt(pageParam);`
- отсутствуют фигурные скобки вокруг тела if: `if (currentpage < 1) currentpage = 1;`
- Комментарий в строке `totalMatches = matchDAO.countByName(name); // почему бы просто не измерять длину листа` предлагает использовать длину списка возвращенных матчей вместо отдельного запроса COUNT. Это неправильно, потому что в запросе на получение списка матчей используется пагинация: мы получаем только одну страницу данных (10 матчей), а общее количество матчей может быть больше. Чтобы построить пагинацию, нужно общее количество матчей, удовлетворяющих условию, а не количество на текущей странице. Поэтому отдельный запрос COUNT необходим.
- ❗️Сейчас из этого сервлета происходит прямое обращение к DAO. В архитектуре приложения обычно стараются разделять уровни ответственности. Прямое обращение к DAO из сервлета может быть допустимо в очень простых приложениях, но в более сложных это приводит к смешиванию слоёв. Это усложняет тестирование и поддержку кода, а также нарушает принцип единственной ответственности. Поэтому лучше внедрить необходимый сервис между сервлетом и DAO.
Цепочка работы должна выглядеть так: `Сервлет → Сервисный слой → DAO → База данных`
- Сервлет не должен знать о том, как получать данные, он должен только делегировать запрос сервису и передавать результат в представление.
Поэтому эта логика должна быть на уровне сервисов
```java
if (name != null && !name.trim().isEmpty()) {
    matches = matchDAO.findByName(name.trim(), currentpage, MATCHES_PER_PAGE);
    totalMatches = matchDAO.countByName(name); // почему бы просто не измерять длину листа
} else {
    matches = matchDAO.findAll(currentpage, MATCHES_PER_PAGE);
    totalMatches = matchDAO.count();
}
int totalPages = (int) Math.ceil((double) totalMatches / MATCHES_PER_PAGE);
```
- ❗️Передача Entity объектов в JSP не является хорошей практикой. Это может привести к проблемам производительности (например, ленивая загрузка) и безопасности (например, случайная передача чувствительных данных). Кроме того, это связывает слой представления с моделью данных. Лучше использовать DTO (Data Transfer Object) для передачи данных в представление. DTO позволяют контролировать, какие именно данные передаются.
- имя параметра `String name = req.getParameter("filter_by_player_name");` можно писать в стиле camelCase

### NewMatchServlet
- из объявления метода `doPost()` можно удалить исключение, которое он не выбрасывает
- малоинформативные имена параметров
```java
String firstPlayerName = req.getParameter("p1");
String secondPlayerName = req.getParameter("p2");
```
- ❗️отсутствует валидация имён игроков. Хотя простая валидация есть на фронтенде — бэкенд про неё ничего не знает и не должен на неё полагаться (фронтенд может измениться). Следует добавить валидацию. 
- этот сервлет уже использует сервисный слой для обработки бизнес-логики и это хорошо. Идеальным вариантом было бы вообще отдать сервису имена игроков (например в DTO) и в ответ получить id созданного матча

### MatchServlet
- в методе `doGet()` нет обработки IllegalArgumentException при `UUID.fromString(uuid)`
- в методе `doGet()` в конце блока `if (match.isEmpty())` можно сделать return и тогда ветку "иначе" можно писать без else
- в методе `doPost()` блок кода 
```java
Match match;
int scoredPlayerNumber = Integer.parseInt(req.getParameter("player"));
if (matchOpt.isPresent()) {
    match = matchOpt.get();
} else {
    throw new RuntimeException("no match in uuid: " + uuid);
}
```
можно написать так
```java
Match match;
if (matchOpt.isEmpty()) {
    throw new RuntimeException("no match in uuid: " + uuid);
}
match = matchOpt.get();
int scoredPlayerNumber = Integer.parseInt(req.getParameter("player"));
```
это уберёт необязательный блок else
- в обоих методах есть дублирующийся код (проверка параметра uuid и получение матча) — можно подумать как этого избежать
- в методе `doPost()` нет проверки параметра uuid на null и пустоту, а также нет обработки IllegalArgumentException при `UUID.fromString(uuid)`
- ❗️в методе `doPost()` при каждом нажатии на кнопку `+ Point` на странице активного матча создаётся новый объект MatchService (и всех объектов, которые создаются в нём) — это избыточное создание объектов. Стоит сделать MatchService полем класса 
- в методе `doPost()` нет обработки NumberFormatException при `Integer.parseInt(req.getParameter("player"));`. Также нет проверки, что число равно `1` или `2`.
- выбрасывание исключения в строке `throw new RuntimeException("no match in uuid: " + uuid);` не информирует пользователя должным образом (т.к. исключения нигде не обрабатываются и не "конвертируются" в понятный для пользователя ответ) — здесь нужно отправить ошибку 404 и прервать выполнение. 
- тип исключения в строке `throw new RuntimeException("no match in uuid: " + uuid);` слишком общий. При реализации логики обработки исключений стоит продумать специализированные классы для них 
- переменную `boolean wasOngoing = !match.isMatchFinished();` точнее было бы назвать просто `ongoing`
- значение переменной `boolean wasOngoing = !match.isMatchFinished();` всегда `true`, поскольку она дублирует условие блока if, в котором объявляется
```java
if (!match.isMatchFinished()){
    boolean wasOngoing=!match.isMatchFinished();
...
}
```
- Сервлет занимается бизнес-логикой проверки состояния матча — можно подумать, как вынести эту логику в сервисный слой.
- в строке `matchService.handlePost(uuid, scoredPlayerNumber);` происходит неявное изменение объекта `Match match` — это нарушает принцип предсказуемости (код должен быть интуитивно понятным и вести себя ожидаемым образом). В текущей реализации можно хотя бы сделать мутацию явной:
```java
Match updatedMatch = matchService.болееИнформативноеИмяМетода(uuid, scoredPlayerNumber);
```

## Test
- `MatchScoreTests` находится в том же пакете, что и `DaoTests` — `daotests`
- Слишком большие тестовые методы нарушают принцип "один тест - один сценарий" и усложняют их чтение. В них также сложнее разобраться, все ли необходимые сценарии проверяются. 
- В тестах тоже следует избегать магических чисел и строк — использовать константы с понятными именами
- Для большей понятности теста (например, `TieBreakTest` и `TieBreakTest2`) можно использовать `@DisplayName`
- Если тест не очевиден, то может (и должен) быть комментарий, объясняющий, зачем он нужен.

## В целом по проекту
- комментарии и особенно закомментированные строки кода лучше удалять для чистоты и чтобы не создавать возможностей запутаться. Если код кажется не понятным без комментария, как например переменная `int gameMode = 1; // 1 - Game; 2 - ADV; 3 - tiebreak;` в классе Score, значит удачная реализация ещё не найдена и стоит продолжать рефакторинг.  
- местами в некоторых классах немного не хватает форматирования. Перед `git commit` можно нажимать (`cmd + alt + l` в Idea на mac os)
- неиспользуемые импорты в классах. Перед `git commit` можно нажимать (`ctrl + alt + o` в Idea на mac os)
- Имена пакетов в java пишут в единственном числе. Когда смотришь на набор классов в пакете, кажется естественным использовать множественное число, обобщая то, что в нём находится, но если посмотреть на декларацию пакета в классе и сравнить варианты, например: `*.validation.limits.annotations.MaxLength` и `*.validation.limit.annotation.MaxLength`, то название в единственном числе становится более логичным как полное имя конкретного класса.
- В коде часто встречаются "магические" переменные — (чаще всего) числа, появляющееся в коде без объяснения своего смысла.
Использование магических переменных является плохой практикой из-за снижения читаемости — приходится догадываться, что означает это число, и сложности поддержки — если значение нужно будет изменить, то придётся искать все его вхождения в коде вручную. 

  А также можно запутаться в том, какой смысл несёт конкретное значение: 
  
  например сейчас в классе ScoreCalculationService (и даже в одном методе) число `2`

  в строке 49 `addGamePointToSet(score, 2);` означает номер игрока, выигравшего очко,

  в строке 52 `score.setGameMode(2); // Больше меньше` — код режима Advantage,

  а в строке 57 `if (score.getFirstPlayerPoints() - score.getSecondPlayerPoints() == 2)` — разницу, необходимую для победы в сете.

  Избежать этого можно используя константы с понятными именами или перечисления (enum) для ограниченного набора значений.

  Хороший пример этого — константа `private static final int MATCHES_PER_PAGE = 10;` в классе MatchesServlet.
- ❗️Во всём проекте отсутствует обработка исключений, поэтому при любой ошибке пользователь видит неинформативную страницу от томката. Следует разработать специализированные исключения, чтобы оборачивать в них возникающие ошибки и обрабатывать их, например, в фильтре, чтобы улучшить пользовательский опыт, доступно информируя о том, что пошло не так.
- ❗️В проекте отсутствуют интерфейсы для ключевых компонентов (DAO и сервисы), что приводит к нарушению принципа инверсии зависимостей (Dependency Inversion). Сейчас сервлеты и сервисы напрямую зависят от конкретных реализаций, что усложняет тестирование и возможную замену реализации в будущем. Было бы правильно выделить интерфейсы для сервисов (возможно) и DAO и внедрять зависимости через них.
- ❗️отсутствуют модификаторы доступа в полях многих классов, что нарушает инкапсуляцию — один из основных принципов ООП. Инкапсуляция требует, чтобы внутреннее состояние объекта было скрыто от внешнего мира. Если поля не объявлены как private, то они могут быть доступны из других классов в том же пакете, что позволяет изменять состояние объекта без контроля. 
- ❗️В текущей реализации не используются DTO, и данные получаются в JSP напрямую из сущностей (например, строка 183 `<td>${match.score.sets[0].first}</td>` и далее в `match.jsp`), поэтому после рефакторинга и переименования, например полей класса Pair, при попытке создания матча приложение выдаст ошибку 500 с сообщением `"Свойство [first] не найдено в типе [com.example.tennisscoreboard2025.models.scoreUtil.Pair]"`. Следует использовать DTO для передачи данных в представление. Это также избавит от рисков LazyInitializationException при передаче Entity в JSP.  
- ❗️объекты DAO и некоторых сервисов создаются внутри класса, в месте объявления. Это делает невозможным подмену реализации и нарушает принцип открытости/закрытости (Open/Closed Principle) — класс должен быть открыт для расширения, но закрыт для изменений. Также классы, зависящие от конкретной реализации DAO невозможно протестировать в отрыве от БД.
  Вместо этого лучше, чтобы сервис зависел от абстракции (интерфейса). Тогда можно будет легко подменять реализацию, не меняя код сервиса.
  Объекты сервисов и DAO могут быть синглтонами или просто создаваться один раз на старте приложения и помещаться, к примеру, в контекст. А в нужном месте их можно получать вместо того, чтобы ещё раз создавать заново.

## Другое
- чтобы визуально протестировать пагинацию на странице списка матчей надо вручную завести много матчей (за 10 дней деплоя столько не набралось) и ещё больше, чтобы протестировать пагинацию при фильтрации по имени игрока. Поэтому было бы хорошо добавлять при старте приложения (или деплое) нужное количество матчей в БД. 

  PS: При написании ревью, я добавил класс, наполняющий БД тестовыми данными, и проверил пагинацию — во всех случаях она работает корректно.
- ❗️в `pom.xml` в зависимости `junit` указана версия `RELEASE`
```xml
<dependency>
    <groupId>junit</groupId>
    <artifactId>junit</artifactId>
    <version>RELEASE</version>
    <scope>compile</scope>
</dependency>
```
Это значит, что Maven будет использовать последнюю доступную версию из репозитория на момент сборки и может привести к тому, что сборка, которая работала вчера, сегодня сломается из-за изменений в новой версии библиотеки. Поэтому лучше указывать явную версию.
- ❗️учётные данные из `hibernate.cfg.xml` 
```xml
<property name="hibernate.connection.username">admin</property>
<property name="hibernate.connection.password">password</property>
```
не должны попадать в github

## Плюсы
- кастомный стиль фронтенда, единый стиль всех страниц
- работающая бизнес-логика
- разделение на слои (Servlet -> Service -> DAO)
- DAO классы инкапсулируют работу с БД
- отдельные запросы COUNT для пагинации
- логичное разделение классов проекта по пакетам
- полноценная пагинация на странице списка матчей
- есть фильтрация матчей по имени игрока (+ возможность поиска по части имени)
- паттерн Singleton для сервиса активных матчей и использование ConcurrentHashMap
- использование Lombok для уменьшения boilerplate-кода
- использование транзакций при работе с БД
- попытка декомпозиции предметной области
- наличие тестов (+ тесты для DAO)
- успешный деплой приложения
- хорошая работа с Hibernate (корректные связи между сущностями, настройка обязательных полей и уникальных ограничений)

## Может быть полезным
- Посмотреть, что такое HttpFilter
- Посмотреть, что такое ServletContextListener
- Посмотреть, записи стримов по декомпозиции в ООП (наиболее полезным для этого проекта может оказаться стрим по игре Орёл и решка)
- Посмотреть, что такое DTO и лучшие практики их использования
- Посмотреть, что такое Проблема N+1 в запросах к БД и способы её устранения
- Посмотреть, что такое MapStruct
- Посмотреть, что такое Hibernate Validator
- Повторить и разобрать на примерах принципы ООП
- Повторить и разобрать на примерах принципы SOLID (для начала DIP)
- Почитать главы, которые привлекут внимание из книги "Рефакторинг" Мартина Фаулера
- Для удобного запуска проекта из Idea CE во время разработки можно настроить плагины Smart Tomcat или Jetty (для Maven проекта)/Gretty (для Gradle)

## Заключение
Проект демонстрирует хорошие навыки разработки. Реализация находится на нормальной для учебного процесса стадии — есть уверенно сильные стороны и остаются возможности для роста как разработчика.

Учебный потенциал в этом проекте ещё не исчерпан — есть важные детали (связанные с критически важными замечаниями), усиление в которых, принесёт ценный опыт. 

Выполнение рекомендаций ещё повысит качество реализации и улучшит твоё понимание и навык построения приложений на Java.

Проект выделяется среди других учебных работ качественной фронтенд-реализацией с кастомным дизайном.
